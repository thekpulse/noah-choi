# 영끌계산기 — 인수인계 문서 (v23.27)

> ## 🔴 첨부물 — **네 개입니다**
>
> | 파일 | 성격 |
> |---|---|
> | **`영끌계산기_지침_v7.md`** | **원칙의 정본. 가장 먼저 읽으세요** |
> | `영끌계산기_인수인계_v23_27.md` | 이 문서 — 현재 상태·할 일 |
> | `yeongkkeul-calculator.html` | **본체 (v23.27)** — `/`가 이걸 가리킵니다 |
> | `test.js` | 회귀 테스트 **397개** (`node test.js`) |
>
> ### 🔴 md 두 개는 GitHub에 올리지 않습니다
> 저장소가 **공개**라 작업 스타일·알려진 구멍·제휴 계획이 그대로 노출됩니다.
> **배포에도 필요 없습니다.** 클라우드에 두고 세션마다 첨부하세요.
>
> | GitHub에 올리는 것 | 안 올리는 것 |
> |---|---|
> | `yeongkkeul-calculator.html` · `rent.html` · `vercel.json` · `test.js` · `api/` | **지침 v7 · 인수인계** |
>
> ⚠ 실거래가 이식(1순위)을 할 때는 **`rent.html`도 함께** 첨부하세요.
>   구 배포판의 유일한 복사본이고, 실거래가 호출부가 그 안에 있습니다.
>
> ### 🔴 이 프로젝트는 계정 두 곳을 오갑니다
> 박사 계정과 노아초이 계정을 오갑니다. **계정에 남는 것에 의존하지 마세요.**
> 프로젝트 지식·메모리·과거 대화는 계정을 넘어가지 않습니다.
> → **작업에 필요한 모든 것은 위 파일들 안에 있어야 합니다.**
>
> ⚠ 박사 계정의 사용자 지침은 산림정책 연구자용입니다. 이 프로젝트와 무관합니다.

> **작성 기준일 2026.08.11 · 판 v23.27**
> v23.17 → … → v23.25 → v23.26 → **v23.27**.
> **계산 엔진·정책 수치는 한 줄도 건드리지 않았습니다.**
> 🔴 **아직 배포하지 않았습니다. 열한 판이 배포 없이 쌓였습니다. 배포가 0순위입니다.**
> ✅ 세션 복구는 `sessionStorage`로 확정·반영(v23.25) · CPO 리뷰 5건 반영(v23.27).
> 🟡 **페이즈 2(캐스케이딩 실거래가)는 착수하지 않았습니다.** 전제조건이 14장에 있습니다.
> 🔴 **v23.19(네온 라임 + 하드 보더)는 폐기됐습니다.** v23.20이 v23.18 문법으로 되돌렸고,
>   v23.21은 그 위에 **개방감 · 하단 조작 위계 · 세로선 폐기 · 카피 다이어트** 넷을 더했습니다.

---

## 🔴 0. 다음 세션에서 가장 먼저 할 일

### A. 두 검사부터 돌립니다

```
node test.js          → 397 / 397 통과해야 합니다
__selfcheck()         → 19항목. 결과 · 입력 03 · 입력 02  — **세 번** 돌립니다
```

🔴 **v23.22부터 `__selfcheck()`를 세 곳에서 돌립니다.**

| 어디서 | 왜 |
|---|---|
| 결과 화면 | 19항목 전부 (G-1·G-7·영수증 합계는 여기서만 잽니다) |
| **입력 03** | **G-14**(선택 테두리) — 결과에는 `.is-on`이 0개라 조용히 통과합니다 |
| 🆕 **입력 02** | **G-16**(입력 필드가 배경에 안 녹는가) — 결과·03에는 `.mfield`가 0개입니다 |

⚠ 측정 대상이 0이면 값 칸에 **🟡 측정 대상 0** 이라고 찍힙니다. **초록이어도 잰 게 아닙니다.**

✅ **v23.26 기준 397 / 397 · `__selfcheck()` 19 / 19 통과 확인했습니다.**
   사보타주 **17종** 전부 🔴 확인. 외부 링크·결과 복원·캐럿·에러 줄까지 브라우저 실측.

⚠ **하나라도 🔴면 다른 작업을 멈추고 그것부터 고칩니다.**
⚠ **`__selfcheck()`를 두 번 돌리는 이유**: `G-14`(선택 상태 테두리)는 `.is-on` 요소를
   세는 검사인데, 결과 화면에는 선택된 칩이 없어 **0개로 조용히 통과**합니다.
   입력 03(지역·주택 보유)에서 한 번 더 돌려야 실제로 검사가 됩니다.
⚠ 판 번호는 화면에서 뺐습니다. `console.info`와 HTML 주석(`<!-- BUILD v23.27 -->`)에 있습니다.

### B. 눈으로 확인할 것

| # | 확인 | 어디 |
|---|---|---|
| 1 | 배경이 **쿨 그레이**, 카드가 **퓨어 화이트 + 은은한 그림자**인지 (누런 톤 0) | 전체 |
| 2 | **검은 테두리 · 오프셋 그림자가 한 군데도 없는지** | 전체 |
| 3 | 하단 독이 **캡슐 없이 가로 2분할**(이전 90px / 다음 나머지)인지 | 01·02·03 |
| 4 | CTA가 **그린 면 + 잉크 글자**인지 | 01·02·03 |
| 5 | 칩·지역 카드 선택이 **흰 면 + 그린 글자 + 뜨는 그림자**인지 | 03 |
| 6 | 선택할 때 칩 **크기가 변하지 않는지** | 03 |
| 7 | 02 부채 입력칸이 **한 칸으로 폭을 다 쓰는지** | 02 |
| 8 | 02 넛지 문구(💡 1억 원당 월 100만 원)가 **옅게** 보이는지 | 02 |
| 9 | **「10억」의 단위가 숨통 있게 떨어져** 보이는지 (짓눌리지 않음) | 01·02 |
| 10 | 결과 「다음 걸음」이 **2×2 바둑판**이고 글자가 안 넘치는지 | 결과 |
| 11 | 부담 타일이 **무채색 → 오렌지 → 딥레드**인지 (**초록 아님**) | 결과 |
| 12 | 자금 구성 막대가 **잉크 + 그린** 두 조각인지 | 결과 |
| 13 | 규제지역 안내가 **옅은 오렌지 면 + 오렌지 글자**인지 | 결과 |
| 14 | 아코디언(부대비용)이 **회색 박스 없이 리스트 줄**처럼 보이는지 | 결과 |
| 15 | 슬라이더 손잡이가 **흰 원 + 부드러운 그림자**인지 (검은 테두리 없음) | 결과 |
| 16 | 최하단 면책이 **두 줄**이고 튀지 않는지 | 결과 |
| 17 | 타이틀 행간 1.15에서 **받침이 겹치지 않는지** | 01·02·03 |
| 18 | 슬라이더·스위치를 만져도 화면이 안 튀는지 | 결과 |
| 19 | 전월세 링크가 어디에도 없는지 | 전체 |
| **20** | 🆕 **하단 「← 이전 단계 / 처음부터 다시 계산하기」가 가로 2분할(35 : 65)**인지 | 결과 |
| **21** | 🆕 **왼쪽 굵은 세로선이 한 군데도 없는지** (규제 안내 · 인테리어 안내 · 결과 이유 줄) | 03 · 결과 |
| **22** | 🆕 규제 안내가 **맑은 오렌지**인지 (탁한 밤색 아님) | 03 · 결과 |
| **23** | 🆕 최하단 면책이 **한 줄**인지 | 결과 |
| **24** | 🆕 「다음 걸음」 네 칸의 **제목이 한 줄**로 들어가는지 (지역명 제거) | 결과 |
| **25** | 타이틀 시작점과 히어로 카드 글자 시작점이 **같은 세로선**에 있는지 | 01 |
| **26** | 🆕 **02·03의 질문도 흰 카드 안**에 있는지 (01과 같은 모양) | 02 · 03 |
| **27** | 🆕 입력칸을 감싸던 **흰 라운드 박스가 없는지** — 흰 면 위에 회색 필드만 | 01 · 02 |
| **28** | 🆕 화면 어디에도 **짙은 녹색 글자가 없는지** (금액·칩·「수정」 전부 잉크) | 전체 |
| **29** | 🆕 선택한 칩·세그먼트가 **1.5px 그린 테두리**인지 (글자색·굵기 변화 없음) | 02 · 03 |
| **30** | 🆕 칩을 **눌렀다 뗄 때 줄이 흔들리지 않는지** | 02 · 03 |
| **31** | 🆕 설명 문구가 입력칸 **아래**에 붙어 있는지 (위 아님) | 02 |
| **32** | 독의 **「이전」 버튼이 보이는지** (배경에 안 녹음) | 01 · 02 · 03 |
| **33** | 🆕 모든 입력칸 위에 **굵은 명사형 소제목**이 있는지 (가진 돈 · 연소득 · 매달 나가는 대출 원리금) | 01 · 02 |
| **34** | 🆕 「대출이 여러 개이신가요?」를 열어 두 칸에 넣으면 **위 칸이 자동으로 합산**되는지 | 02 |
| **35** | 🆕 위 칸을 직접 고치면 **항목 두 칸이 비워지는지** | 02 |
| **36** | 🆕 💡 이모지와 「1억 원당 월 100만 원」 문구가 **없는지** | 02 |
| **37** | 🆕 「처음부터 다시 계산하기」가 **「다음」과 같은 그린 버튼**인지 (검정 아님) | 결과 |
| **38** | 🆕 「이전 단계」에 **화살표가 없는지** | 결과 |
| **39** | 화면 어디에도 **얇은 글씨(300)가 없는지** | 전체 |
| **40** | 🆕 상단이 **아이콘 없이 「영끌계산기」 워드마크**만인지 (누르면 처음으로) | 전체 |
| **41** | 🆕 하단 CTA · 옵션 칩 · 상태 칩의 **높이가 화면마다 같은지** (54 / 46 / 35px) | 전체 |
| **42** | 🆕 결과 하단 **「ⓘ 계산 기준 보기」**를 누르면 바텀시트가 올라오는지 | 결과 |
| **43** | 🆕 시트를 **닫기 · 배경 탭 · 뒤로가기(ESC)** 셋으로 닫을 수 있는지 | 결과 |
| **44** | 🆕 시트를 닫은 뒤 **뒤 화면이 그대로 스크롤되는지** (굳지 않음) | 결과 |
| **45** | 부대비용 금액 칸을 눌렀을 때 **화면이 확대되지 않는지** (iOS) | 결과 |
| **46** | 02까지 입력하고 **새로고침**하면 값과 단계가 그대로 돌아오는지 | 02 |
| **47** | **탭을 닫았다 다시 열면 값이 없는지** (남으면 안 됩니다) | 전체 |
| **48** | 「처음부터 다시 계산하기」 뒤 새로고침하면 **빈 화면**인지 | 결과 → 01 |
| **49** | 🆕 부담률이 20% 넘으면 **오렌지**, 35% 넘으면 **다크 레드**로 바뀌는지 | 결과 |
| **50** | 🆕 위험 색이 **네온 레드가 아닌 절제된 다크 레드**인지 | 결과 |
| **51** | 🆕 2줄 타이틀의 **받침이 아랫줄과 붙지 않는지** (행간 1.22) | 01·02·03 |
| **52** | 하단 두 버튼의 **글자 크기가 같은지** | 결과 |
| **53** | 🆕 「실거래가 확인하기」를 누르면 **새 탭**에서 열리는지 (같은 탭 ❌) | 결과 |
| **54** | 🆕 새 탭에서 돌아온 뒤 **결과 화면이 그대로** 있는지 | 결과 |
| **55** | 🆕 결과 화면에서 **새로고침**해도 결과가 유지되는지 | 결과 |
| **56** | 🆕 리포트 이미지가 **밝은 톤**인지 (검정 아님) · 그린 세로 막대가 보이는지 | 결과 |
| **57** | 🆕 만 칸에 **6자리**를 넣으면 안내 줄이 **부드럽게** 나타나는지 | 01·02 |
| **58** | 🆕 「12,345」 가운데를 고칠 때 **커서가 끝으로 튀지 않는지** | 01·02 |

⚠ **17번은 어떤 검사도 못 봅니다.** 행간 1.15 + 자간 -.05em + 900 조합입니다.
   「빌」·「짧」·「꿇」이 든 2줄 타이틀을 추가하면 반드시 실기로 확인하세요.
⚠ **6번은 검사가 부분적으로만 봅니다.** G-14는 결과 화면에서 `.is-on`이 0개라
   조용히 통과합니다. **입력 03에서 `__selfcheck()`를 한 번 더 돌리세요.**
⚠ 캐시가 남으면 주소 뒤에 `?v=2327`을 붙이세요.

### C. 다음 판 순서

| 순위 | 항목 | 상태 |
|---|---|---|
| **0** | **v23.27 배포** | 🔴 **아직 안 올렸습니다 — 열한 판이 쌓였습니다** |
| **1** | **실거래가 API 연동** | 🔴 `rent.html`에 호출부·UI가 그대로 있음. **이식만** |
| **2** | **정책대출 UI** (디딤돌·신생아특례·보금자리론) | 🔴 엔진(`POLICY_DEFS`)은 이식됨. **화면만 없음** |
| 3 | 상세 입력 (금리·기간·MCI·은행 자체한도) | 🔴 5.4% · 30년 고정 |
| ~~4~~ | ~~전월세 탭~~ | ⏸ **본체에서 제거** — `rent.html`은 보관 (6장) |
| 5 | 상세 입력이 붙으면 `test.js`에 케이스 추가 | 9장 |

### D. 배포 구조

| 주소 | 실제 파일 |
|---|---|
| `/` | **`yeongkkeul-calculator.html`** ← 대표 |
| `/rent` | `rent.html` (본체에서 링크하지 않음) |

```json
{ "cleanUrls": true,
  "rewrites": [
    { "source": "/",     "destination": "/yeongkkeul-calculator.html" },
    { "source": "/rent", "destination": "/rent.html" } ] }
```

🔴 **구 배포판(v21.30)은 덮어썼습니다.** 되돌리려면 GitHub 커밋 히스토리에서 꺼내야 합니다.
   다만 **실거래가 코드는 `rent.html`에 그대로 남아 있어** 기능상 잃은 것은 없습니다.

### E. 올리기 전 고쳐야 할 두 줄

```js
const PARTNER = { interior:'', moving:'' };          // 제휴 확정 시 교체
const SERVICE_URL = 'https://noah-choi.vercel.app';  // 공유 문구에 붙는 주소
```

---

## 1. 🆕 v23.27 — 배포 전 마감 3건

검사 **380 → 397개**. **사보타주 17종 전부 🔴 확인.**

| # | 이슈 | 결과 |
|---|---|---|
| 1 | 외부 링크 후 뒤로가기 → 계산 유실 | ✅ **앵커 전환 + 결과 화면까지 복원** |
| 2 | 공유 카드만 다크 톤 | ✅ 라이트 톤 · 그린은 브랜드 마크로만 |
| 3 | 입력 피드백 (콤마 · 에러) | ✅ **캐럿 보존** + 잘림 안내 줄 신설 |

### 1-1. 🔴 「돌아오면 초기화된다」 — 원인이 둘이었습니다

**(a) 링크가 `<button>` + `window.open`이었습니다.**
`noopener`는 붙어 있었지만, **카톡·인스타 인앱 브라우저는 스크립트로 연 창을 막거나 같은 탭에 띄웁니다.**
같은 탭에 뜨면 뒤로가기에서 페이지가 다시 로드됩니다.

→ 밖으로 나가는 넷(`outNaver`·`outHogang`·`outInterior`·`outMove`)을
  **진짜 `<a target="_blank" rel="noopener noreferrer">`**로 바꿨습니다. `href`만 채우고 `onclick`은 쓰지 않습니다.
  `window.open`과 `go()`는 **정의부터 삭제**했습니다.

**(b) 다시 로드돼도 결과 화면으로 돌아가지 않았습니다.**
v23.25는 입력만 복원하고 **결과는 일부러 버렸습니다**(「역산은 한 번만」).
그래서 돌아온 사람은 03단계에 서서 「결과 확인하기」를 다시 눌러야 했습니다.

→ **그 판단을 뒤집었습니다.** `onResult`와 `lockedPrice`를 저장하고, 돌아오면 결과로 바로 복원합니다.

> `lockedPrice`는 **이미 한 번 정한 값**입니다. 같은 탭에서 그대로 돌려주는 것은
> 역산을 **다시 부르는 것이 아닙니다.** 오히려 버리고 재계산하면 그때 원칙이 깨집니다.

⚠ 안전장치 다섯을 함께 넣었습니다.

| 장치 | 왜 |
|---|---|
| `lockedPrice`가 **유한한 양수일 때만** 복원 | 손으로 고친 값이 들어올 수 있습니다 |
| `ready()`를 통과해야 복원 | 입력이 깨졌으면 퍼널로 돌립니다 |
| `render()`가 **항상 `onResult=false`** | 안 내리면 영영 결과로만 돌아갑니다 |
| `priceOverride`는 **저장하지 않음** | 「다른 집값으로 보기」는 그 자리에서만 유효합니다 |
| `pyeong`·`interior`도 범위 검사 | 원칙 122 — 되살린 값은 입력값입니다 |

→ **실측:** 결과 화면에서 새로고침 → `result.hidden=false` · `lockedPrice` 동일 · 금액 「3억 9,700만원」 그대로.

### 1-2. 공유 카드 — 웹과 같은 톤으로

| | v23.26 | **v23.27** |
|---|---|---|
| 상단 면 | 검정 그라데이션 `#000 → #191F28` | **`--bg` #F2F4F6** |
| 글자 | 흰색 | **`--ink` / `--ink-3`** |
| 브랜드 | `--bronze-hi` 회색 글자 | **그린 세로 막대** + `--ink-4` 글자 |
| 금액 | 흰색 800 | **`--ink` 900** |

> **브랜드는 공유되는 곳에서 먼저 읽힙니다.** 카톡에서 받은 사람이 보는 첫 화면이 이 카드입니다.
> 웹은 밝은데 카드만 검으면 **다른 서비스로 보입니다.**

⚠ **그린을 글자로 쓰지 않는 락(v23.22)은 여기서도 유효합니다.**
   「시그니처 그린을 숫자에」라는 지시를 그대로 붙이면 `#00CA71`이 흰 면 위 **1.97 : 1**입니다.
   대신 화면의 `.headline::before`와 **같은 문법**(그린 세로 막대)으로 브랜드를 세웠습니다.

⏹ `--grad-1` · `--grad-2` · `--bronze-hi` 세 토큰을 **정의부터 삭제**했습니다. 색 총 **21 → 20개**.

### 1-3. 입력 피드백 — 캐럿과 에러 줄

**콤마는 딜레이가 없었습니다.** 다만 **캐럿이 튀는 문제**가 있었습니다.

```
「12,345」의 가운데(1|2)에 9를 입력
  전:  값 12,934 · 커서가 문자열 끝(6)으로 이동
  후:  값 12,934 · 커서 4  ← 방금 친 9 바로 뒤
```

`setCommaValue()`가 **「콤마 앞의 숫자 개수」**를 세어 같은 자리로 되돌립니다.
오른쪽 정렬이라 뒤에 붙여 쓸 때는 티가 안 나지만, **고쳐 쓸 때 커서가 날아갔습니다.**

**잘림을 화면으로 말합니다.** 이전에는 조용히 잘렸습니다(`slice`).

| 자리 | 안내 |
|---|---|
| 억 5자리 | 억 단위는 9,999까지 넣을 수 있어요. |
| 만 6자리 | 만 단위는 99,999까지 넣을 수 있어요. |
| 부채 5자리 | 월 상환액은 9,999만원까지 넣을 수 있어요. |

`.fielderr` — 헬퍼와 같은 자리·크기, 색만 `--bad`. `opacity` + `max-height` 전환(0.18s).

⚠ **`hidden`으로 껐다 켜면 전환이 재생되지 않습니다.** 항상 DOM에 두고 클래스로 폅니다.
⚠ 접힌 상태의 `max-height`가 0이라 **평소에는 한 픽셀도 차지하지 않습니다.**
⚠ `role="alert" aria-live="polite"` — 스크린리더에도 전달됩니다.

---

## 1-A. (이전 판) v23.26 — CPO 리뷰 (섹션 3) 반영

검사 **372 → 380개**. **사보타주 13종 전부 🔴 확인.**
지시 5건 중 **4건 반영 · 1건은 이미 충족**. 그리고 그 과정에서 **판정 색이 한 번도 안 뜨던 버그**를 잡았습니다.

| # | 지시 | 결과 |
|---|---|---|
| 1 | 위험도 인디케이터를 절제된 다크 레드로 | ✅ + 🔴 **임계값 버그 동시 수정**(1-1장) |
| 2 | `.headline` 행간 1.22 고정 | ✅ `.q-title`도 함께 |
| 3 | 하단 2분할 버튼 폰트 통일 | ✅ 14px → 16px |
| 4 | UX 라이팅 정제 | ✅ 4곳 |
| 5 | 미니멀리즘 · 점진적 노출 유지 | ✅ 이미 충족 — G-1·G-2가 잠그고 있습니다 |

### 1-1. 🔴 「블랙으로 보인다」의 진짜 원인 — 경고 색이 한 번도 뜨지 않았습니다

> 「고위험 구간이 블랙으로 렌더링됩니다. 절제된 다크 레드로 바꿔 주세요」

**고위험 색은 원래 레드였습니다.** 화면이 검게 보인 건 **부담률이 항상 「낮음」으로 판정됐기 때문**입니다.

```js
게이지 막대 폭 =  ratio / 0.40 * 100    ← DSR 상한 40%를 만석으로
색 단계        =  ratio < 0.40 ? ok : ratio < 0.60 ? mid : bad
```

**두 눈금이 어긋나 있었습니다.** 막대가 꽉 찬(ratio 0.40) **뒤에야** 색이 바뀌는 구조입니다.
게다가 한도는 스트레스 금리(6.9%)로 잡고 상환액은 실제 금리(5.4%)로 계산하므로
**DSR을 꽉 채워도 화면 부담률은 최대 34%**입니다.

> 즉 `mid`·`bad` 두 색은 **구조적으로 도달 불가능**했습니다. 언제나 무채색(`#333D4B` ≈ 검정)이었습니다.
> 이전 판 주석에 그 사실이 적혀 있었는데도 임계는 그대로였습니다.

**임계를 막대와 같은 눈금으로 맞췄습니다.**

| 막대 | 부담률 | 색 |
|---|---|---|
| 50% 미만 | ~20% | `--ok` 무채색 |
| 50~87.5% | 20~35% | `--warn` 오렌지 |
| 87.5% 이상 | 35%~ | `--bad` 다크 레드 |

→ **실측:** 부담 33%가 검정 → **오렌지**로 바뀌었습니다.
⚠ 경고가 **더 자주** 뜨는 방향입니다. 원칙 28(사용자에게 유리한 오차)에서 안전한 쪽입니다.

🔒 락을 「임계가 40/60인가」에서 **「색 경계가 막대 만석 안쪽에 있는가」**로 다시 썼습니다.
   값이 아니라 **관계**를 잠급니다. 「경고 두 색이 도달 가능한가」도 함께 봅니다.

### 1-2. 🔴 위험 색 — 두 번째로 방향을 뒤집었고, 이번엔 **양쪽에 벽을 세웠습니다**

| 판 | `--bad` | 채도 | 지적 |
|---|---|---|---|
| ~v23.20 | `#7F2420` | **91** | 「탁한 밤색」 |
| v23.21~25 | `#C00F0C` | **177** | 「네온 레드」 |
| **v23.26** | **`#9B3028`** | **107** | — |

흰 면 위 **7.38 : 1** · 색상각 4° · `--warn`(0.1598)보다 어두움(0.0924).

> **v23.21의 락은 하한(채도 ≥110)만 걸었습니다.** 탁한 색을 막으려고 만든 락인데,
> 그 락 때문에 반대편 끝(순색 177)까지 갔고 이번에 「네온」이라는 지적을 받았습니다.
> **한쪽만 막으면 다음 판에 반대편으로 튑니다.**

🔒 새 락은 **채도 95 ~ 150** 양방향입니다. 밤색 91도, 네온 177도 여기서 걸립니다.
   「위험이 주의보다 어둡다(0.75배 이하)」도 함께 잠급니다.

### 1-3. 타이틀 행간 — 문장 1.22 / 숫자 1.15

| 대상 | 행간 | 왜 |
|---|---|---|
| `.headline` · `.q-title` | **1.22** | 900 + 자간 -.05em + 25px에서 2줄이면 **윗줄 받침이 아랫줄에 붙습니다** |
| `.mfield input` · `.rhead-amount` · `.tile-v` | **1.15** | 받침이 없고, 벌리면 **큰 금액이 두 덩어리로 흩어집니다** |

v23.19의 「타이틀·금액 행간 1.1~1.2 한 값」 락을 **두 값으로** 다시 썼습니다.
「문장 행간 > 숫자 행간」도 함께 잠급니다 — 뒤집히면 받침 충돌이 그대로 돌아옵니다.

⚠ 인수인계 0장 B-17에 **「어떤 검사도 못 본다」고 적어 두었던 위험**입니다. 이제 봅니다.

### 1-4. UX 라이팅 — 네 곳

| 자리 | 전 | 후 |
|---|---|---|
| 02 헬퍼 | 세전 기준이에요. 부부라면 두 사람 소득을 합쳐서 넣어주세요. | **대출 한도는 소득으로 정해져요.** 세전 기준 · 부부는 합산해서 넣어주세요. |
| 한도 안내 | **정확한** 금액은 은행 상담으로 | **실제** 금액은 은행 상담으로 |
| 대출 없이 | 예상 예산이 **크게** 올라가요 | 예상 금액이 올라가요 |
| 조건 칩 | 연소득 **안 넣음** | 연소득 **넣지 않음** |

- **「정확한」**은 면책(「실제 금액은 은행 심사에 따라 다름」)과 서로 어긋납니다
- **「크게」**는 수치 없는 정도 표현입니다
- **마지막 줄은 G-8 위반**이었습니다 — 같은 상태를 `'안 넣음'`과 `'넣지 않음'` 두 이름으로 부르고 있었습니다

🔒 「화면 문구에 과장·모호 표현이 없다」가 금지어 7종을 훑고,
   「소득 미입력의 이름이 하나다」가 G-8을 잠급니다.

### 1-5. 하단 두 버튼 · 미니멀리즘

- `.reedit-cta` 14px → **16px** (`.restart-cta`와 동일). 위계는 **면**이 만듭니다(흰 면 ↔ 그린 솔리드)
- 미니멀리즘은 이미 **G-1(첫 뷰포트 금액 ≤6)·G-2(카드 ≤3)·G-9(결과 블록 ≤8)**가 잠그고 있습니다.
  v23.24의 바텀시트가 점진적 노출의 그릇입니다 — **새 옵션은 전부 그 안으로 들어갑니다**

---

## 1-B. (이전 판) v23.25 — 세션 상태 복구 (`sessionStorage` 확정)

```js
const DRAFT_KEY  = 'yk.draft.v1';
const DRAFT_KEYS = ['step','cash','income','noLoan','houseStatus','group','sido','sgg',
                    'debtMonthly','debtParts','debtOpen','debtPartsOpen'];
```

| 저장한다 | 저장하지 않는다 |
|---|---|
| 입력값 · 단계 · 펼침 상태 | **`lockedPrice`(결과 집값)** · `priceOverride` · 평형 · 인테리어 |

🔴 **결과는 복구하지 않습니다.** `lockedPrice`를 항상 `null`로 되돌립니다 —
   결과는 「입력이 끝난 시점에 한 번만 산정」해야 합니다.

🔴 **`localStorage`는 계속 막습니다.** 원칙 36의 취지는 「기기에 남기지 않는다」였고
   `sessionStorage`는 탭과 함께 사라지므로 취지가 유지됩니다. **락을 반쪽만 푼 것입니다.**

🔴 **되살린 값은 믿지 않습니다.** `HOUSE[]` · `sggName()` · `LAWD[]` · `step` 범위를 다시 검사합니다.
   ⚠ `zoneFromSgg()`는 모르는 코드에 `'other'`를 돌려주므로 **존재 검사로 못 씁니다.**
     `'99999'`가 「수도권 밖」으로 통과했습니다.

**실측:** 새로고침 후 전 항목 복원 · `step:99`→2 · `'해킹'`→`none` · `'99999'`→`''` ·
`restart()` 후 키 없음 · `localStorage` 키 0개 · 에러 0건.

⚠ `restart()` 직후 `render()`가 **빈 초안을 다시 쓰고 있었습니다.** 넣은 게 없으면 키를 안 만듭니다.
⚠ 사파리 프라이빗 모드는 접근에서 던집니다. 저장·삭제·복구 **세 함수 전부 `try/catch`**입니다.

---

## 1-C. (이전 판) v23.24에서 바뀐 것 — CPO 리뷰

검사 **346 → 364개**. **사보타주 18종 전부 🔴 확인**(원칙 99).

| # | 지시 | 결과 |
|---|---|---|
| 1 | 헤더 아이콘 제거 · 타이포만으로 | ✅ **워드마크가 홈 버튼**이 됐습니다 |
| 2 | 컴포넌트 3계층 규격 | ✅ `--h-cta` 54 / `--h-opt` 46 / `--h-chip` 35px 토큰 고정 |
| 3 | 점진적 정보 공개 (ⓘ + 바텀시트) | ✅ 신설 |
| 4 | 세션 상태 복구 (로컬 저장) | 🟡 **구현 보류 — 승인 대기** (1-5장) |
| 4 | 바이럴 루프 완성도 | ✅ 공유·복사·이미지 저장 3종 이미 있음. 이모지만 정리 |

### 1-1. 헤더 — 아이콘을 지우고 워드마크에 홈을 얹었습니다

```
[영끌계산기]                                    (결과)
 900   500
```

- 라인 집 아이콘(`.iconbtn`) **정의까지 삭제**했습니다
- `영끌`(900 · `--ink`) + `계산기`(500 · `--ink-3`), 자간 `-.04em`
- **워드마크 자체가 홈 버튼**입니다(`id="homeBtn"` 유지) — 아이콘을 지우면서 기능을 잃지 않았습니다
- 단계 라벨(`.barlabel`)은 오른쪽 끝으로 옮기고 **결과 화면에서만** 켜집니다. 비면 자리도 안 먹습니다

🔒 검사 셋 — 「헤더에 아이콘이 없다」·「워드마크가 홈 버튼이다」·「굵기 대비 ≥ 300」.

### 1-2. 컴포넌트 3계층 — 높이를 토큰으로 못 박았습니다

| 계층 | 토큰 | 높이 | 글자 | 어디 |
|---|---|---|---|---|
| 1 메인 CTA | `--h-cta` | **54px** | 16px **600** | 다음 · 결과 확인 · 처음부터 · 이전 단계 · 시트 닫기 |
| 2 옵션 · 선택 | `--h-opt` | **46px** | 14px 700 | 칩 · 지역 카드 · 세그먼트 |
| 3 상태 칩 · 뱃지 | `--h-chip` | **35px** | 13px | 조건 칩 · ⓘ 계산 기준 |

⚠ **이 셋 밖의 높이를 새로 만들지 마세요.** 버튼이 네 종류가 되는 순간 규격이 아닙니다.
   검사 「버튼 높이를 px로 하드코딩한 곳이 없다」가 여덟 셀렉터를 훑습니다.

🔴 **덤으로 잡은 것 둘.**

| 자리 | 무엇이 틀렸나 |
|---|---|
| `--t7: 12.5px` | 지침이 금지한 **0.5px 단위**였습니다(모서리·자간과 같은 규칙) → **13px** |
| `.costrow .amt input` 14px | **iOS Safari는 16px 미만 입력에 포커스하면 화면을 확대합니다.** 지침의 「폼 컨트롤 16px 이상」을 이 칸만 어기고 있었습니다 → **16px** |

두 번째는 v23.23에서 새로 만든 **항목별 합산 입력 두 칸도 그대로 물려받고 있었습니다.**
검사 「모든 텍스트 입력이 16px 이상이다」가 `input`이 든 모든 규칙의 실제 px를 계산해 봅니다.

### 1-3. 점진적 정보 공개 — ⓘ + 바텀시트

```
결과 하단:  [ ⓘ 계산 기준 보기 ]      ← 35px 상태 칩
              ↓ 탭
            ┌──────────────────────┐
            │  계산 기준            │
            │  스트레스 DSR         │
            │  취득세 · 지방교육세   │
            │  중개보수             │
            │  방공제(최우선변제금)  │
            │  정책 확인일           │
            │  [ 닫기 ]             │
            └──────────────────────┘
```

기존의 「스트레스 DSR · 취득세 · 지방교육세 · 중개보수 상한요율 기준 반영」 **한 줄을 대체**했습니다.
**지운 게 아니라 한 단계 뒤로 옮긴 것**입니다 — 그 한 줄은 용어만 나열해서 사실 아무것도 설명하지 않았습니다.

| 규칙 | 왜 |
|---|---|
| **시트는 `.app` 밖에 둡니다** | `.app{overflow-x:clip}`이 자손 `position:fixed`를 가둡니다 |
| **닫는 길이 셋** | 닫기 버튼 · 배경 탭 · ESC |
| **스크롤 잠금은 `documentElement.style.overflow`만** | `body{position:fixed}` 기법은 **스크롤 위치를 튕깁니다**(원칙 92) |
| `hidden` 해제 → 다음 프레임에 `.on` | 같은 프레임에 붙이면 transform 전환이 재생되지 않습니다 |
| 열 때 포커스 이동 · 닫을 때 복귀 | 스크린리더·키보드 사용자를 위한 최소한 |

🔴 **면책 한 줄은 시트로 옮기지 않았습니다.** 방어 문구는 눌러야 보이면 안 됩니다.

### 1-4. 이모지 · 카피

지시 밖이지만 v23.23의 「화면 이모지 0개」 락이 새 시트에도 걸립니다 — 시트 문구는 사실만 서술합니다.
ⓘ는 이모지가 아니라 **문자 기호(U+24D8)**라 락에 걸리지 않습니다(검사 범위 확인 완료).

### 1-5. 🟡 세션 상태 복구 — **승인이 필요합니다. 이번 판에 넣지 않았습니다**

> 「로컬 스토리지 기반의 임시 상태 복구 기능을 백그라운드에 필수로 장착」

**지금 코드에는 저장이 한 줄도 없고, 그것을 검사가 잠그고 있습니다**(원칙 36·56).

```js
tt('localStorage·sessionStorage 미사용', !/localStorage|sessionStorage/.test(noComment));
```

이 락은 「금액·소득을 기기에 남기지 않는다」는 결정이었습니다.
**깨는 것 자체는 문제가 아닙니다. 다만 이건 구조·프라이버시 변경이라 승인을 받고 갑니다**(작업 스타일 3번).

| | A. `sessionStorage` | B. `localStorage` |
|---|---|---|
| 새로고침 복구 | ✅ | ✅ |
| 뒤로가기 복구 | ✅ | ✅ |
| 탭 닫았다 다시 열기 | ❌ | ✅ |
| 기기에 남는 기간 | **탭 닫으면 소멸** | 지울 때까지 |
| 가족·공용 기기에서 소득이 보일 위험 | **없음** | **있음** |

**권장은 A입니다.** 지적하신 실패(입력 도중 새로고침·뒤로가기)는 A가 전부 막습니다.
탭을 닫았다 며칠 뒤 돌아오는 사람에게까지 소득을 남겨 둘 이유는 이 도구에 없습니다.

B로 가신다면 함께 넣어야 하는 것: **만료(예: 24시간)** · **결과 화면의 「저장된 값 지우기」** ·
**첫 복구 시 「이어서 하시겠어요?」 확인**. 셋 없이 B만 넣으면 원칙 36의 취지가 통째로 없어집니다.

→ **A / B 중 하나를 말씀해 주시면 다음 판에서 바로 넣고, 락을 새 규칙으로 다시 쓰겠습니다.**

### 1-6. 바이럴 루프 — 이미 있습니다

| 기능 | 상태 |
|---|---|
| 카톡 한 줄 요약 복사 (`outCopy`) | ✅ `navigator.share` → `clipboard` → `execCommand` 3단 폴백 |
| 리포트 이미지 저장 (`outSave`) | ✅ html2canvas · 440px 카드 · 실패 시 토스트 |
| 네이버 부동산 · 국토부 실거래가 | ✅ 딥링크 (⚠ 8장 — **아직 실검증 못 했습니다**) |

v23.24에서는 토스트 문구의 이모지만 뺐습니다. **인터랙션 자체는 손대지 않았습니다.**

---

## 1-D. (이전 판) v23.23에서 바뀐 것 — 출시 전 최종 감사

C-Level 감사 6개 항목을 반영했습니다. **계산 엔진·정책 수치는 무변경입니다.**
검사 **329 → 346개**. **사보타주 21종 전부 🔴 확인**(원칙 99).

### 1-0. 여섯 항목 처리 결과

| # | 지시 | 결과 |
|---|---|---|
| 1 | 브랜드 그린 단일화 · 텍스트 녹색 금지 | ✅ **v23.22에서 이미 완료.** 이번 판에 확인만 |
| 2 | 폼 3단 위계 · 명사형 레이블 | 🆕 **`.flabel` 신설.** 모든 입력칸에 적용 |
| 3 | 복수 대출 자동 합산 아코디언 | 🆕 **신설.** 신용대출 · 기타 두 칸 → 위 칸으로 실시간 합산 |
| 4 | 재계산 CTA 그린화 · 화살표 제거 | 🆕 반영 |
| 5 | 타이포 (자간·행간·굵기) | 🆕 **Light(300) 전면 폐기** · 나머지는 이미 충족 |
| 6 | 💡 넛지 삭제 · 중첩 베젤 제거 | 🆕 넛지 삭제(3번이 대체) · 베젤은 v23.22에서 완료 |

⚠ **1번과 6번 후반부는 이미 되어 있었습니다.** 외부 감사는 v23.21 화면을 본 것으로 보입니다.
   **지운 것을 다시 지우지 않도록** 매 판 이 문서를 먼저 확인하세요(지침 5층).

### 1-1. 🆕 폼 3단 위계 — 예외 없음

```
[ .flabel  볼드 명사형 소제목 ]   15px · 800 · --ink        margin-top 22px
[ .mfield  단일 입력 필드     ]                              margin-top  8px
[ .helper  서브 헬퍼 텍스트   ]   12.5px · 400 · --ink-4     margin-top  6px
```

| 화면 | 라벨 | 헬퍼 |
|---|---|---|
| 01 | **가진 돈** | 주택담보대출을 제외하고 끌어모을 수 있는 모든 자금이에요. |
| 02 | **연소득** | 세전 기준이에요. 부부라면 두 사람 소득을 합쳐서 넣어주세요. |
| 02 부채 | **매달 나가는 대출 원리금** | 신용대출·자동차·학자금 등 매달 나가는 원리금이에요. |

🔴 **라벨은 「답한 것」 요약 줄의 이름과 **같은 값**을 씁니다**(G-8).
   `STEPS`의 `key`가 `가진 돈`·`연소득`이므로 라벨도 그대로 씁니다.
   여기가 어긋나면 같은 값이 화면에서 두 이름으로 불립니다. 검사가 두 목록을 대조합니다.

⚠ **간격은 안쪽이 바깥쪽보다 좁아야 묶입니다.** 라벨 위 22px > 라벨 아래 8px = 필드 아래 6px.
   이 관계가 깨지면 라벨이 위 덩어리에 붙어 보입니다. 검사가 네 값의 **관계**를 봅니다.

⏹ **값이 있으면 사라지던 동적 힌트를 없앴습니다.** 설명은 `.helper` 하나로 고정입니다.
   문구가 나타났다 사라지면 입력할 때마다 줄 수가 흔들립니다.

### 1-2. 🆕 복수 대출 자동 합산 — 「머릿속 덧셈」을 없앴습니다

```
매달 나가는 대출 원리금
[                    125 만 ]        ← 자동으로 채워집니다
신용대출·자동차·학자금 등 …
1년이면 1,500만원을 갚고 있어요…
[+30만][+50만][+100만][지우기]
─────────────────────────────
대출이 여러 개이신가요? 항목별로 더하기   ▾
   신용대출                    [  80 만 ]
   기타 대출                   [  45 만 ]
   마이너스통장 · 자동차 · 학자금 등
```

**실측 확인:** 80 + 45 → 메인 칸 `125`, `S.debtMonthly = 125`.

| 규칙 | 왜 |
|---|---|
| **값의 출처는 `S.debtMonthly` 하나** | 항목은 「합계를 만드는 보조 입력」이지 두 번째 진실이 아닙니다(원칙 84) |
| **엔진으로 가는 값은 그대로** `otherDebtMonthly:(S.debtMonthly||0)*10000` | 백엔드 로직 무수정 — 지시대로 |
| **위 칸을 직접 고치면 항목 두 칸을 비웁니다** | 안 비우면 「합계 200인데 항목은 80+45」인 상태가 남습니다 |
| 빠른 추가 칩을 눌러도 항목을 비웁니다 | 위와 같은 이유. 두 자리 모두에서 비웁니다 |
| **토글은 화면을 다시 그리지 않습니다** | `renderQuestion()`을 부르면 입력 포커스가 날아갑니다 |

⚠ **이 기능은 4층 관문을 통과했습니다.** 지우는 것(💡 넛지)과 넣는 것이 정확히 교환됐습니다 —
   넛지는 「머릿속으로 더하라」는 요구를 남긴 채 어림값만 준 것이었고, 이 아코디언이 그 요구 자체를 없앱니다.

### 1-3. 🆕 CTA 일관성 — 지침 6-5의 예외 조항을 폐기했습니다

| | v23.22 | **v23.23** |
|---|---|---|
| 처음부터 다시 계산하기 | 검정 `--espresso` + 흰 글자 | **`--green` + 잉크 글자** (01·02·03의 「다음」과 동일) |
| 이전 단계 | `← 이전 단계` | **`이전 단계`** (화살표 삭제) |

> **프라이머리 액션이 화면마다 다른 색이면, 그 색은 더 이상 「액션」을 뜻하지 않습니다.**
> 지침 6-5의 「재계산 CTA는 예외로 검정 면」 조항은 여기서 폐기합니다.
> 검사 「프라이머리 버튼이 화면마다 같다」가 `.cta`와 `.restart-cta`의 배경 **토큰을 대조**합니다.

⚠ 버튼 안의 화살표는 **위치가 이미 말하는 것**을 반복합니다. 왼쪽에 있는 버튼이 「이전」입니다.

### 1-4. 🆕 Light(300) 전면 폐기

| 자리 | v23.22 | v23.23 |
|---|---|---|
| `.subline` · `.rhead-label` · `.rhead-tail` · `.u` · `.tile-s` · `.line .k` · `.mini span` | **300** | **400** |

> **한글 300은 12~17px에서 획이 끊겨 보입니다.** 「힘을 뺀 것」이 아니라 **「흐린 것」**으로 읽힙니다.
> 대비는 **400 ↔ 900** 두 단으로 만듭니다. 검사가 `font-weight:300`을 **한 곳도 못 쓰게** 막습니다.

핵심 수치(`.rhead-amount`·`.tile-v`·`.mfield input`·`.line.total .v`)는 **전부 700 이상**임을 따로 잽니다.

### 1-5. 🔴 화면에서 이모지를 0개로

지시는 02단계의 💡 하나였지만, 세어 보니 **다섯 곳**이었습니다.

| 자리 | v23.22 | v23.23 |
|---|---|---|
| 02 넛지 | `💡 계산이 어렵다면? …` | ⏹ 삭제 (아코디언이 대체) |
| 공유 문구 | `… 선이에요! 💡` | `… 선이에요.` |
| 토스트 4종 | `✅ 복사했어요` · `⚠ 실패…` | `복사했어요` · `저장에 실패했어요. 다시 시도해 주세요` |

⚠ **`__selfcheck`의 ✅·🔴·🟡는 콘솔 진단이라 세지 않습니다.** 검사가 그 구간을 잘라내고 봅니다 —
   경계를 안 그으면 **검사가 자기 자신을 보고 영원히 🔴**입니다.

### 1-6. 🔴 `--fill`을 `#F2F4F6`으로 — **정적 방어선 하나를 잃었습니다**

입력 필드 면을 지시대로 `#F2F4F6`으로 맞췄습니다(같은 요구가 **두 판 연속**이라 반영했습니다 — 지침 5층).

**⚠ 이 값은 `--bg`와 같습니다.** v23.18에 「0만만 허공에 뜨던」 사고가 난 바로 그 조건입니다(원칙 97).

| | v23.22 | **v23.23** |
|---|---|---|
| 정적 G-15 | `--fill ≠ --bg` · `--card ≠ --bg` | **`--card ≠ --bg` · `--card ≠ --fill`** (한 축 상실) |
| 렌더 G-16 | 보조 | **유일한 방어선** |

**지금 이것이 성립하는 조건은 딱 하나 — 모든 `--fill` 면이 흰 카드 안에만 놓이는 것**입니다.
`--fill` 면을 흰 카드 밖으로 내보내려면 **먼저 G-16을 통과시키세요.**

곁가지로, 앱 배경 위의 호버(`.iconbtn`·`.arow`·`.cta.back`·`.reedit-cta`·`.mini.amber`)가
`--fill`이면 **아무 일도 일어나지 않습니다.** 전부 `--line`으로 바꿨고 검사로 잠갔습니다.

### 1-7. 🔴 이번에도 검사가 헛돌았습니다 — `indexOf`의 -1

「모든 입력 필드가 라벨 → 입력 → 헬퍼 순서다」를 처음 이렇게 썼습니다.

```js
mc.indexOf('class="flabel"') < mc.indexOf('class="mfield"')
```

**`indexOf`는 없으면 -1을 돌려줍니다. 그리고 -1 < n 은 언제나 참입니다.**
라벨을 **통째로 지운 사보타주가 조용히 통과**했습니다.

→ 존재(`>= 0`)부터 세고 그다음 순서를 보도록 고쳤습니다.
→ **순서를 재는 검사는 「있는가」를 먼저 물어야 합니다.** 원칙 99의 새 형태입니다.

---

## 1-E. (이전 판) v23.22에서 바뀐 것 — 디자인 시스템 일관성

제미나이 4개 원칙을 반영했습니다. **계산 엔진·정책 수치는 무변경입니다.**
검사 **313 → 329개**, `__selfcheck` **17 → 19항목**. **사보타주 23종 전부 🔴 확인**(원칙 99).

### 1-0. 네 원칙 요약

| # | 지시 | 실제 반영 |
|---|---|---|
| 1 | 브랜드 그린 단일화 | **`--green-ink`(#00794A)를 정의부터 삭제.** 그린은 면·선 전용, 글자는 잉크 |
| 2 | 입력 필드 3중첩 타파 | **`.moneycard`의 흰 베젤 삭제** + 질문 블록을 01·02·03 모두 같은 흰 카드로 |
| 3 | 헬퍼 텍스트 위계 | `.helper` 컴포넌트 신설 — **필드 아래 6px**, 12.5px, 쿨 그레이 |
| 4 | 테두리 전역 규칙 | 입력 `border:0` · 선택 3종(칩·지역카드·세그먼트) **폭 1.5px 고정, 색만 전환** |

### 1-1. 🔴 그린을 하나로 — 원칙 102의 답을 바꿨습니다

v23.15부터 그린이 두 값이었습니다. `--green`(면) / `--green-ink`(글자).
원칙 102는 **「그린을 글자로도 쓰려면 두 값이 필요하다」**였습니다.

**v23.22의 답은 더 강합니다 — 그린을 글자로 아예 쓰지 않습니다.**

| 자리 | v23.21 | v23.22 |
|---|---|---|
| 히어로 금액 | 짙은 녹색 `#00794A` | **잉크 `#191F28`** |
| 선택 칩 글자 | 짙은 녹색 | **잉크** (선택은 그린 **테두리**가 말합니다) |
| 세그먼트 선택 글자 | 짙은 녹색 | **잉크** |
| 「수정」 링크 | 짙은 녹색 | `--ink-3` |
| 아코디언 열림 | 짙은 녹색 | 캐럿만 잉크로 진해짐 |
| 스위치 켜짐 · 체크박스 | 짙은 녹색 면 | **`--green` 면** (체크 표시는 잉크) |
| CTA 호버 | 짙은 녹색으로 어두워짐 | **색 안 바뀜** — `filter:brightness(.94)` + 그림자 |

⚠ **CTA 호버가 문제였습니다.** 그린이 하나뿐이면 「더 짙은 그린」이 존재하지 않습니다.
   색을 바꾸는 대신 **밝기와 그림자로만** 반응하게 했습니다. 잉크 글자는 그대로라 7.64:1이 유지됩니다.

🔒 검사 세 개가 잠급니다 — 「그린 토큰이 단 하나다」·「그린을 글자색으로 쓰지 않는다」·
   **「그린은 면·선으로만 쓰인다」**(세 번째는 `background`/`border-color`/`outline` 외의 속성에
   `var(--green)`이 붙으면 어디든 잡습니다).

### 1-2. 🔴 3중첩 타파 — 다만 **원인이 지시와 달랐습니다**

> 「02페이지 숫자 입력칸을 감싼 거대한 흰색 라운드 박스를 삭제하고,
>  **하얀색 배경 위에** 옅은 회색 입력 필드만 단독으로 배치하라」

진단은 정확합니다. 다만 **02·03의 배경은 흰색이 아니라 쿨 그레이(`--bg`)였습니다.**
흰 베젤만 지우면 회색 필드(`--fill` #EEF0F3)가 회색 배경(#F2F4F6) 위에 놓여
**1.02 : 1로 통째로 사라집니다** — v23.18에서 실제로 겪은 사고입니다(원칙 97).

**왜 01만 멀쩡했나**를 보면 답이 나옵니다. 01은 히어로 카드가 **흰 면**이라
그 안에서 `.moneycard`가 이미 벗겨져 있었습니다. 즉 **지시가 원하는 모양은 01에 이미 있었습니다.**

| | v23.21 | **v23.22** |
|---|---|---|
| 01 | 회색 → **흰 카드** → 회색 필드 (2단) | 그대로 |
| 02 · 03 | 회색 → 질문(맨몸) → **흰 카드** → 회색 필드 (**3단**) | 회색 → **흰 카드** → 회색 필드 (2단) |

→ 히어로 전용이던 `.app.hero-on .funnel > .q` 규칙을 **`.funnel > .q`로 올려** 01·02·03을 통일하고,
  `.moneycard`는 **간격만 담당**하게 남겼습니다(`background:transparent; padding:0; box-shadow:none`).

⚠ **입력 필드 색은 지시의 `#F2F4F6` 대신 `--fill`(#EEF0F3)을 유지했습니다.**
   `#F2F4F6`은 우리 `--bg`와 **같은 값**이라, 쓰는 순간 G-15(원칙 97)가 무너집니다.
   두 값의 차이는 ΔE≈1로 눈에 안 보이고, **관계(흰 면 위 옅은 회색)는 지시와 동일**합니다.

🔒 **G-16 신설** — 필드의 **부모를 거슬러 올라가 실제로 칠해진 면**을 찾아 대비를 잽니다.
   토큰만 봐서는 안 보이는 종류입니다. 1.05:1 미만이면 🔴.

### 1-3. 테두리 전역 규칙 — 폭은 **고정**, 색만 바뀝니다

| 대상 | 기본 | 활성 |
|---|---|---|
| 입력 필드 `.mfield` | **`border:0`** + `--fill` 면 | 포커스는 **`outline`** (레이아웃 무영향) |
| 칩 `.chip` | `1.5px solid var(--line)` | **`border-color:var(--green)`** |
| 지역 카드 `.zonecard` | 〃 | 〃 |
| 세그먼트 `.seg button` | `1.5px solid transparent` | 〃 |
| 조건 칩 `.condchip` | `1px solid var(--line)` 🆕 | — |

⚠ **지시는 「기본 1px → 활성 1.5px」였습니다. 폭을 고정했습니다.**
   폭이 0.5px 변하면 상자가 1px 커져 **칩 줄이 다시 흐릅니다**(원칙 34 · G-14).
   **활성 사양(1.5px 그린)은 그대로 지키고 기본을 1.5px로 올렸습니다.**
   `#E5E8EB`는 1.5px에서도 헤어라인으로 읽힙니다 — 이쪽이 훨씬 싼 대가입니다.

⚠ **포커스를 `border`로 내면 안 됩니다.** 두 칸 그리드(억/만)에서 필드가 커지며 옆 칸을 밀어냅니다.
   `outline`은 레이아웃을 전혀 건드리지 않습니다. 검사가 이 자리를 잠급니다.

### 1-4. 🔴 G-17이 **5판 묵은 버그**를 잡았습니다 — 선택하면 칩이 1px 커집니다

테두리 폭을 고정하고 나서도 칩 상자가 움직였습니다.

```
선택 전 93.61px  →  선택 후 94.63px   (+1.02px)
```

원인은 테두리가 아니라 **글자 굵기**입니다. `.chip{font-weight:700}` → `.chip.is-on{font-weight:800}`.
**굵어진 글자가 칩을 넓혔습니다.** v23.16부터 그대로 있었고, **G-14는 테두리 폭만 재서 못 봤습니다.**

→ 선택 상태의 `font-weight` 선언을 셋(칩·지역카드·세그먼트) 다 지웠습니다.
  선택 신호는 이미 셋입니다 — **그린 테두리 · 뜨는 그림자 · 잉크로 진해지는 글자.**

🔒 **G-17 신설** — 보이는 칩을 실제로 켰다 껐다 하며 **렌더된 폭**을 잽니다(허용 0.5px).
   소스 검사도 하나 더 걸었습니다(「선택해도 글자 굵기를 바꾸지 않는다」).

### 1-5. 헬퍼 텍스트 컴포넌트

```
[ 입력 필드 ]
  ↓ 6px
설명 문구        .helper   12.5px · --ink-4 · 행간 1.6
  ↓ 4px
💡 어림값 넛지   .q-hint   같은 컴포넌트
```

- 「신용대출·자동차·학자금 등…」을 필드 **위 → 아래**로 옮겼습니다
- `.readout`(금액 미리보기)도 마진 14px → **6px**로 당겨 필드에 붙였습니다
- 죽은 규칙 `.debtbox > .q-sub{margin:0}` 삭제

🔴 **색은 지시(`#8B95A1`) 대신 `--ink-4`(#6B7684)를 유지했습니다.**

| 값 | 흰 면 위 대비 | 판정 |
|---|---|---|
| `#8B95A1` (지시) | **3.04 : 1** | 🔴 본문 기준(4.5:1) 미달 |
| `#6B7684` (현재) | **4.62 : 1** | ✅ AA |

**v23.20에서 정확히 같은 이유로 한 번 내린 값입니다**(그 판의 1-5장).
12.5px 문장은 마이크로 라벨이 아니라 **본문**이라 기준이 4.5:1입니다.
색상 계열(쿨 그레이)과 위계(본문보다 한 단 아래)는 지시대로입니다.

> ⚠ **한 번만 말합니다.** 그래도 `#8B95A1`로 가겠다고 하시면 그대로 바꾸고
>   이 표를 근거로 문서에 남기겠습니다(지침 5층).

### 1-6. 🔴 검사 자체의 사고 둘 — 이번에도 나왔습니다

**(a) `__selfcheck` 항목 검사가 주석만 봐도 통과했습니다.**

```js
['G-13','G-14','G-15','G-16','G-17'].forEach(k => tt(..., html.includes(k)));
```

`G-17`은 **코드 주석에도 적혀 있습니다.** 검사 본문을 통째로 지우는 사보타주가
주석 때문에 **초록으로 통과**했습니다.
→ 지금은 `ok('...')` 호출의 **라벨만 모아서** 봅니다. 검사가 실제로 도는지를 봅니다(원칙 90 · 99).

**(b) `그린을 글자색으로 쓰지 않는다` 검사가 정반대로 헛돌았습니다.**

`/color:var\(--green\)/`는 **`border-color:var(--green)`도 잡습니다.**
새로 만든 그린 테두리 3종이 전부 걸려 검사가 처음부터 🔴였습니다.
→ 경계(`(?:^|[;{\s])`)를 붙여 고쳤습니다.

**(c) 🔴 그리고 화면이 통째로 죽는 사고 하나.**
`__selfcheck` 안에서 `const probe`를 **두 번 선언**했습니다(G-13이 이미 쓰고 있었습니다).
`SyntaxError`가 나면서 **스크립트 전체가 죽어 첫 화면이 백지로 떴습니다.**

> **`node test.js`는 이걸 못 봅니다.** 화면 코드를 실행하지 않기 때문입니다.
> **브라우저에서 한 번 띄워 보는 것으로만 잡힙니다.** 9장의 3층 분업이 왜 필요한지의 사례입니다.

### 1-7. 덤으로 정리한 파편

| 자리 | 무엇이 어긋나 있었나 |
|---|---|
| `.costrow` · `.report-line` | **선을 `--fill`(면 토큰)로** 그리고 있었습니다 → `--line` |
| 독의 `이전` 버튼 | `--fill` 면이라 앱 배경과 **1.02 : 1** — 사실상 안 보였습니다 → 흰 면 + 헤어라인 |
| `.condchip` | 테두리 0 인데 `:hover{border-color:...}`만 있었습니다 → `1px solid var(--line)` |
| 질문 카드만 테두리 | `.card`·`.tile`은 그림자뿐인데 `.q`만 헤어라인 → **그림자 하나로 통일** |
| `.cta.ghost` · `.textbtn` | 어떤 마크업도 안 쓰는 **죽은 규칙** → 삭제 (원칙 84) |

색 총 **22 → 21개** (상한 26).

---

## 1-F. (이전 판) v23.21에서 바뀐 것 — 개방감 · 하단 위계 · 세로선 폐기

제미나이 4개 도메인 지시를 전량 반영했습니다. **계산 엔진·정책 수치는 무변경입니다.**
검사 **293 → 313개** (신규 20 · 개정 2). **신규 20개 전부 사보타주 16종으로 확인했습니다**(원칙 99).

### 1-0. 네 도메인 요약

| # | 지시 | 실제 반영 |
|---|---|---|
| 1 | 레이아웃 개방감 | `--pad` **24 → 20px** · 히어로 카드 패딩도 토큰화 · `.brand` 좌우 **24 → 18px** |
| 2 | 하단 CTA 가로 2분할 | `.restartrow` **flex 35 : 65** · 높이 56px 통일 · 좌 세컨더리 / 우 솔리드 |
| 3 | 시각 노이즈 제거 | **`border-left` 전면 삭제(4곳)** · `--warn`·`--bad`를 **순색으로 교체** · 틴트 면으로 위계 |
| 4 | 카피 다이어트 | 면책 **2줄 → 1줄** · 「다음 걸음」 지역명 제거 · 작은 안내 행간 **1.5 → 1.6** |

### 1-1. 🔴 지시의 진단은 맞고 **원인이 다른 곳이었던 자리** — 개방감

> 「메인 화이트 카드의 **좌우 바깥 여백(Margin)**을 대폭 줄여라 (예: 좌우 16px 수준)」

**바깥 여백은 이미 16px이었습니다.** 폭을 먹고 있던 것은 **카드 자신의 패딩 24px**입니다.

| | 360px 화면에서 내용 폭 |
|---|---|
| v23.20 | 360 − 32(바깥) − **48(카드 패딩)** = **280px** |
| **v23.21** | 360 − 32(바깥) − **40** = **296px** |

바깥 여백을 더 줄이면 카드가 화면 끝에 붙어 「인셋 카드」라는 형태 자체가 사라집니다.
**지시의 목표(내부 입력칸이 숨 쉬는 폭)는 카드 패딩으로 달성했습니다.**

⚠ **덤으로 잡힌 것 하나** — `.brand`(24px) + 타이틀 세로선(18px) = 42px 이었고
   카드 글자 시작점은 16 + 24 = 40px 이었습니다. **2px 어긋나 있었습니다.**
   지금은 18 + 18 = **36px** = 16 + 20 = **36px**로 정확히 맞습니다.
   검사 「타이틀과 카드 글자의 시작점이 맞는다」가 **세 값의 관계로** 잠급니다.

### 1-2. 하단 조작 버튼 — 세로 2단 → 가로 2분할

| | v23.20 | v23.21 |
|---|---|---|
| 배치 | 54px + 58px **세로 2단** (여백 포함 ≈ 122px) | **가로 2분할 56px 한 줄** |
| 비율 | 둘 다 100% | **35 : 65** |
| 문구 | 「← 이전 단계로 돌아가기」 | **「← 이전 단계」** |
| 위계 | 흰 면 / 솔리드 | 그대로 (흰 면 = 세컨더리 / 솔리드 = 프라이머리) |

⚠ **「옅은 배경의 Secondary」를 `--fill`로 읽으면 안 됩니다.** `--fill`(#EEF0F3)은
   앱 배경 `--bg`(#F2F4F6)와 **1.02 : 1**이라 버튼이 통째로 사라집니다(원칙 97 — v23.18에서 이미 한 번 겪었습니다).
   여기서 「옅은 세컨더리」는 **흰 면 + 헤어라인 + 그림자**입니다.
   검사 「세컨더리 면이 앱 배경과 다른 값」이 이 자리를 잠급니다.

⚠ **락 하나를 다시 썼습니다**(지침 5층 3번). 「되돌아가기 문구에 방향 표시가 있다」의
   `/← 이전 단계로 돌아가기/` → `/← 이전 단계/`. **지우지 않고 기준만 옮겼습니다.**
   35% 폭에 긴 문구를 넣으면 두 줄이 되거나 잘립니다(원칙 34).

### 1-3. 🔴 세로선 폐기 — 「선」을 지운 자리는 **반드시 「면」이 받습니다** (원칙 101)

`border-left`가 있던 네 자리를 전부 지웠습니다.

| 자리 | v23.20 | v23.21 |
|---|---|---|
| `.tip` (한도·방공제 안내) | 회색 3px 세로선 | **선도 면도 없음** — 글자만 |
| `.tip.info` (규제지역 안내) | 오렌지 3px 세로선 + 틴트 | **틴트 면 + 전체 라운드** |
| `.overnote` (가진 돈 초과) | 오렌지 3px 세로선 | **`--warn-tint` 면** |
| `.rhead-why` (결과 이유 줄) | `::before` 3px 세로선 | **평상시 `--ink-tint` / 한도 도달 시 `--warn-tint`** |

⚠ **`.headline::before`(타이틀 세로선)는 남깁니다.** 그것은 「안내 박스의 인용문 선」이 아니라
   **브랜드 마크**이고, 검사 「타이틀 세로선이 있다」로 잠겨 있습니다. 지시의 대상이 아닙니다.

🔴 **선을 지우면 분리 근거가 하나 줄어듭니다.** 그래서 지운 만큼 틴트를 새로 정의했고,
   「틴트 두 값이 정의돼 있다」·「경고 틴트가 --warn에서 파생됐다」 둘로 잠갔습니다.
   **틴트를 지우면 안내 박스가 본문에 녹습니다.**

### 1-4. 🔴 판정 색 — 사다리 방향은 그대로, **탁한 흙색만 순색으로**

> 「칙칙하고 탁한 오렌지/밤색 대신 애플 스타일의 맑고 채도 높은 순색을 쓰라」

원칙 104(무채 → 유채 채도 사다리)는 **그대로 유지**하고, 유채 두 단의 **채도만** 올렸습니다.

| 토큰 | v23.20 | **v23.21** | 흰 면 대비 | 채도 `R − max(G,B)` | 색상각 |
|---|---|---|---|---|---|
| `--ok` | `#333D4B` | **그대로** | 11.00 : 1 | 무채 | — |
| `--warn` | 탁한 오렌지 | **`#BE4B00`** | **5.00 : 1** | 83 → **115** | 24° |
| `--bad` | 탁한 버건디 | **`#C00F0C`** | **6.33 : 1** | 91 → **177** | 1° |
| `--warn-tint` | — | **`rgba(190,75,0,.06)`** | — | `--warn`에서 파생 | — |
| `--ink-tint` 🆕 | — | **`rgba(25,31,40,.04)`** | — | 중립 안내 면 | — |

⚠ **애플 System Orange(`#FF9500`)·System Red(`#FF3B30`)는 그대로 못 씁니다.**
   흰 면 위 **2.20 : 1 · 3.55 : 1**로 AA 미달입니다. 애플이 밝은 배경용으로 따로 내놓은
   **Accessible 변형**(오렌지 `#C93400` 계열)이 지시가 가리키는 값입니다.
   `#BE4B00`은 4.5:1을 지키면서 낼 수 있는 **가장 채도 높은 오렌지**입니다 — 채도 115가 사실상 상한입니다.
   그래서 검사 임계를 **110**으로 잡았습니다(이전 두 값은 83·91이라 여기서 떨어집니다).

⚠ **틴트는 반드시 `--warn`에서 파생합니다.** v23.20의 1-5장에서 이미 겪은 자리입니다 —
   `--warn`만 바꾸고 틴트를 두면 **면과 글자의 색상이 어긋납니다.** 이제 검사가 세 채널을 대조합니다.

### 1-5. 카피 다이어트

| 자리 | v23.20 | v23.21 |
|---|---|---|
| 면책 | **2줄** (같은 말을 두 번) | **1줄** — 「※ 본 결과는 시뮬레이션 추정치이며, 정부 정책 변화 및 은행 심사 기준에 따라 실제 대출 한도와 세금은 변동될 수 있습니다.」 |
| 다음 걸음 ① | `서울 성동구 14.7억대 추천 단지 보기` | **`14.7억대 추천 단지 보기`** |
| 다음 걸음 ② | `서울 성동구 실거래가 확인하기` | **`실거래가 확인하기`** |
| 작은 안내 행간 | 1.5 (`.readout`·`.mini span`·`.costrow small`) / 1.4 (2×2) | **전부 1.6 이상** |
| 보조 글자 굵기 | 500 | **400** (`.readout`·`.costrow small`) |

⚠ **지역명 제거의 근거는 「사용자가 안다」가 아니라 「바로 위 조건 칩이 이미 말한다」**입니다.
   결과 헤더의 조건 칩에 `지역 서울 성동구`가 항상 떠 있습니다(원칙 43).
   실기 캡처로 확인했습니다 — Pretendard 기준 네 칸 제목이 **전부 한 줄**에 들어갑니다.

### 1-6. 🆕 덤으로 지운 것 — 죽은 토큰 3개

`--glass` · `--glass-line` · `--glass-2`. **어떤 규칙도 `var()`로 부르지 않았습니다.**
G-6(색 ≤ 26)의 정원만 먹고 있었습니다. 지금 색 총 **22개**입니다.

> **죽은 CSS는 죽은 값이 아니라 「살아 있는 두 번째 정의」입니다**(원칙 84 · 지침 6-6).
> v23.18의 `.mrow` 6줄과 같은 종류입니다.

### 1-7. 🔴 이번에도 검사가 헛돌았습니다 — 원칙 99, 세 판 연속

「바깥 여백이 16px 인셋」 검사를 처음 이렇게 썼습니다.

```js
css.match(/\.funnel\{padding:\d+px (\d+)px/)
```

그런데 `@media (max-width:400px)` 안의 `.funnel{padding:22px 12px 32px}`가
**소스에서 먼저 나옵니다.** 12px를 읽고 통과해 버렸습니다.

→ 줄머리(`\n\.funnel\{`)로 앵커해 고쳤습니다.
→ **v23.20의 `.moneycard` 사고와 똑같은 형태입니다.** 그때는 하위 셀렉터가,
   이번에는 **미디어 쿼리 안의 같은 셀렉터**가 앞에 있었습니다.
→ **CSS 검사는 줄머리 앵커가 기본값이어야 합니다.** 원칙 99에 이 형태를 덧붙였습니다.

---

## 1-G. (이전 판) v23.17 → v23.20에서 바뀐 것 — 전부 화면 전용

제미나이 피드백 3회분을 전량 반영했습니다. **계산 엔진·정책 수치는 무변경입니다.**

| 판 | 내용 | 검사 |
|---|---|---|
| v23.18 (1차) | 레이아웃·색 5건 + 타이포·여백 2건 | 247 → 266 |
| v23.18 (마무리) | `__selfcheck`에 G-13·G-14 신설 | 266 → 268 |
| **v23.20 (3차)** | **레퍼런스 동기화 — 팔레트 전면 교체** | **268 → 283** |

### 1-1. v23.18에서 한 것 (요약)

| # | 내용 |
|---|---|
| 1 | CTA를 화사한 그린 + 잉크 글자로 (5.48 → 7.64:1) |
| 2 | 02 부채 입력칸을 `.moneycard` 안으로 (배경에 녹아 사라지던 문제) |
| 3 | 「← 이전 단계로 돌아가기」 버튼화 |
| 4 | 선택 상태의 2px 녹색 테두리 폐기 |
| 5 | 팥죽색 `#C4837C` 폐기 · 「수정」 어포던스 |
| 6 | `button`·`input`에 `letter-spacing:inherit` — **전역 자간이 닿지 않고 있었음** |
| 7 | 타이틀·금액 자간 -.05em 통일 · 질문↔카드 간격 24px 통일 |
| 8 | 죽은 `.mrow` 6줄 삭제 (금액 자간의 **두 번째 정의**가 숨어 있었음) |

### 1-2. v23.19 — 레퍼런스 동기화 (⏹ **폐기됨**)

네온 라임 `#C6EF4E` + 웜 베이지 `#EAEADF` + 1px 검정 테두리 + `3px 3px 0` 오프셋 섀도우.
**한 판 만에 폐기됐습니다** — 「금융 앱의 신뢰감을 훼손하는 B급 감성」.

⚠ **되돌리지 않고 남긴 것이 있습니다**(지침 원칙 103). 아래 1-4장.

### 1-3. 🆕 v23.20 — 안티 브루탈리즘 · 정제된 모던 UI

| # | 항목 | v23.19 | v23.20 |
|---|---|---|---|
| 1 | 배경 / 카드 | `#EAEADF` / `#F8F7F7` | **`#F2F4F6` / `#FFFFFF`** |
| 1 | 강조 | `#C6EF4E` 네온 라임 | **`#00CA71` / `#00794A` 핀테크 그린** |
| 1 | 카드 분리 | 1px 검정 테두리 **1개** | **배경색 차이 + 앰비언트 섀도우 2개** |
| 1 | 떠 있는 요소 | `3px 3px 0 #000` | **`0 10px 30px rgba(0,0,0,.08)`** |
| 2 | 단위 여백 | `margin-left:-1px` | **`+4px`** (숨통) |
| 2 | 금액 자간 | -.05em | **-.04em** (굵기 900 유지) |
| 3 | 하단 독 | 둥근 캡슐 | **가로 2분할** (이전 90px / 다음 `flex:1`) |
| 4 | 부담 낮음 | 그린 `#00794A` | **무채색 `#333D4B`** |
| 4 | 부담 중간 | 브릭 레드 `#B33A3A` | **딥 오렌지 `#A85510`** |
| 4 | 규제지역 안내 | 투명 + 헤어라인 | **옅은 오렌지 면 + 오렌지 글자** |
| 4 | 02 넛지 카피 | — | **신설** (1억 원당 월 100만 원 어림값) |
| 5 | 슬라이더 손잡이 | 그린 + 검정 2px | **흰 원 + 부드러운 그림자** |
| 5 | 아코디언 | 회색 박스 | **투명 면 + 헤어라인** |
| 6 | 면책 | 1줄 | **2줄** (정책 변동 고지 추가) |

**색 총 16 → 21개** (상한 26). rgba 그림자가 돌아왔습니다.

### 1-4. 🔴 되돌리면서 **남긴 것** — 원칙 103

되돌리는 판의 진짜 위험은 색이 왔다 갔다 하는 게 아니라
**그 판에서 얻은 개선까지 함께 지우는 것**입니다. 아래는 전부 살렸습니다.

| v23.19에서 얻은 것 | v23.20에서 |
|---|---|
| 02 부채칸 1단 100% | **유지** |
| 다음 걸음 2×2 바둑판 | **유지** |
| 타이틀·금액 굵기 900 · 행간 1.15 | **유지** |
| `--fill ≠ --bg` (G-15) | **유지** — 값만 `#EEF0F3`으로 |
| 막대 대비 검사가 실제 두 조각을 봄 | **유지** |
| G-14(선택해도 두께 불변) | **유지** |

⚠ **이 판단은 검사가 대신해 주지 않습니다.** 되돌리는 사람이 목록을 만들어야 합니다.

### 1-5. 🔴 지시를 그대로 붙이면 깨지던 자리 둘

| 처방 | 그대로 쓰면 | 실제 반영 |
|---|---|---|
| 넛지 카피 색을 `--ink-4`로 | 13px 본문이 흰 면 위 **3.04 : 1** — AA 미달 | `--ink-4`를 `#6B7684`로 한 단 내려 **4.62 : 1** |
| 규제 안내에 `rgba(179,58,58,.05)` | `--warn`이 오렌지로 바뀌어 **면과 글자의 색상이 어긋남** | `--warn-tint`를 `--warn`에서 파생 |

`--ink-4`는 v23.19까지 마이크로 라벨 전용이라 3.04:1로도 통했습니다.
**넛지가 본문 문장이라서 기준이 올라간 것**입니다 — 색을 바꾼 게 아니라 쓰임이 바뀐 자리입니다.

### 1-6. 🔴 판정 색 방향을 두 번째로 뒤집었습니다

| 판 | 부담 낮음 | 근거 |
|---|---|---|
| ~v23.17 | 팥죽색 `#C4837C` | 레드 농도 3단 |
| v23.18~19 | 그린 `#00794A` | 「부담 18%는 실제로 좋은 상태」 |
| **v23.20** | **무채색 `#333D4B`** | **초록은 「좋다」가 아니라 「해도 된다」로 읽힘** |

v23.18의 판단이 틀린 게 아니라 **한 걸음 부족했습니다.**
부담 게이지에 초록을 칠하면 「빚을 더 내도 된다」는 신호가 되고,
이 도구에서 그것은 **원칙 28(사용자에게 유리한 오차)에 걸리는 방향**입니다.

→ **지침 원칙 104**로 승격했습니다. 명도 사다리가 아니라 **채도 사다리**입니다.

## 2. 시각 언어 (v23.27 확정) — 쿨 그레이 · 핀테크 그린 · 앰비언트 섀도우

| 토큰 | 값 | 역할 |
|---|---|---|
| `--bg` / `--card` | `#F2F4F6` / `#FFFFFF` | 쿨 그레이 바탕 · 퓨어 화이트 카드 |
| `--fill` | **`#F2F4F6`** | 입력칸·트랙 면 — 🔴 **`--bg`와 같은 값.** 흰 카드 안에서만 씁니다 (1-D-6장) |
| `--line` | `#E5E8EB` | 헤어라인 · 트랙 · 비활성 면 |
| `--green` | `#00CA71` | **채널 A — 유일한 브랜드 그린. 면·선 전용** (v23.22) |
| ~~`--green-ink`~~ | ⏹ **삭제** | 짙은 녹색 글자를 없앴습니다. 되살리지 마세요 |
| `--ink` / `--ink-2` / `--ink-3` / `--ink-4` | `#191F28` / `#333D4B` / `#4E5968` / `#6B7684` | 글자 4단 |
| `--espresso` / `--espresso-2` | `#191F28` / `#000000` | 솔리드 다크 면 (재계산 CTA) |
| `--ok` / `--warn` / `--bad` | `#333D4B` / `#BE4B00` / `#9B3028` | **채널 B — 무채 → 오렌지 → 다크 레드** · 채도 **95~150** 양방향 잠금 |
| ~~`--grad-1`·`--grad-2`·`--bronze-hi`~~ | ⏹ **삭제** | 공유 카드가 라이트 톤이 되며 쓸 자리가 없어졌습니다 (v23.27) |
| `--warn-tint` | **`rgba(190,75,0,.06)`** | 규제지역 · 초과 안내 면 — ⚠ `--warn`에서 파생 |
| `--ink-tint` 🆕 | `rgba(25,31,40,.04)` | 중립 안내 면 (결과 헤더의 이유 줄) |
| 제휴 | **색 없음** — 흰 면 + 얇은 윤곽 + `↗` | 채널 C |
| `--hair` | `1px solid #E5E8EB` | 헤어라인 |
| `--sh` / `--sh-lift` | `0 4px 20px rgba(0,0,0,.03)` / `0 10px 30px rgba(0,0,0,.08)` | 앰비언트 섀도우 |

🔴 **`--edge`·`--hard`·`--hard-sm`(하드 보더 문법)은 정의부터 삭제했습니다.**
   검사 「오프셋 하드 섀도우 전면 폐기」·「카드에 굵은 검정 테두리가 없다」·「웜 베이지 잔재 없음」이 잠급니다.

### 명암비 실측

| 조합 | 값 | 판정 |
|---|---|---|
| CTA 잉크 on 그린 | **7.64 : 1** | AAA |
| CTA 호버 흰 on 딥그린 | 5.48 : 1 | AA |
| 자금 구성 막대 두 조각 | **7.64 : 1** | ✅ 원칙 94 |
| 부담 낮음(무채) on 흰 | 11.00 : 1 | AAA |
| 주의 오렌지 `#BE4B00` on 흰 | **5.00 : 1** | AA |
| 위험 다크 레드 `#9B3028` on 흰 | **7.38 : 1** | AAA |
| `--ink-4` on 카드 / 배경 | 4.62 / 4.19 : 1 | AA |
| 카드 on 배경 | 1.10 : 1 | **그림자가 보완** |

### 🔴 분리 근거 세기 (원칙 101)

| 판 | 카드가 배경에서 분리되는 근거 | 하나 지우면 |
|---|---|---|
| v22.2 | 없음 (둘 다 흰색) | ❌ 실제로 뭉개져서 되돌림 |
| v23.19 | 검은 테두리 **1개** | ❌ 카드가 통째로 사라짐 |
| **v23.20** | **배경색 차이 + 앰비언트 섀도우 2개** | 하나 잃어도 버팀 |

### 자간 · 굵기 규약 — **두 단**입니다

| 대상 | 자간 | 굵기 | 행간 |
|---|---|---|---|
| 전역 (body · **button · input**) | -.02em | — | 1.5 |
| **문장** (`.q-title` · `.headline`) | **-.05em** | **900** | **1.22** ← v23.26 |
| **숫자** (`.mfield input` · `.rhead-amount` · `.tile-v`) | **-.04em** | **900** | **1.15** |
| 중간 금액 (`.line.total .v` 20px) | -.04em | 800 | — |
| 단위 (`억`·`만`) | -.02em | 700 | `margin-left:4px` |

⚠ **숫자와 문장은 다른 자간을 씁니다**(지침 6-9). 한글 문장은 붙어도 단어로 읽히지만,
   **숫자는 붙으면 떡집니다** — 자릿수를 세야 하기 때문입니다.
⚠ **`button`·`input`은 `letter-spacing`을 상속하지 않습니다.** `inherit` 명시 필수 — **G-13**이 잠급니다.
✅ **한글 2줄 타이틀의 받침 충돌은 v23.26에서 행간 1.22로 해결했습니다.**
   「문장 타이틀 행간이 1.22다」·「문장 행간 > 숫자 행간」 두 검사가 잠급니다.

- **카드 패딩 `20px`** (v23.21 · 이전 24px) · 카드 간격 `16px` · **질문 → 입력 카드 `24px`**
- **바깥 여백 `16px`** (≤400px에서 12px) · `.brand` 좌우 `18px` — 타이틀·카드 글자 시작점을 36px로 맞춘 값
- **하단 독**: 캡슐 없음. `flex` 2분할 — 이전 `0 0 90px` / 다음 `flex:1`
- 🆕 **결과 하단 조작**: `.restartrow` `flex` 2분할 — 이전 단계 `35` / 재계산 `65` · 높이 56px
- 🆕 **`border-left` 금지.** 안내 박스의 위계는 **틴트 면**으로만 만듭니다 (예외: `.headline::before` 브랜드 마크)
- 🆕 **질문 블록 `.funnel > .q`는 01·02·03 모두 흰 카드.** `.moneycard`는 간격만 담당합니다(면·패딩·그림자 0)
- 🆕 **테두리 전역 규칙** — 입력 `border:0` (+`--fill` 면, 포커스는 `outline`),
  선택 3종(칩·지역카드·세그먼트) **폭 1.5px 고정 · 색만 `--line` ↔ `--green`**
- 🆕 **선택 상태는 글자 굵기를 바꾸지 않습니다.** 글자가 넓어져 상자가 커집니다(G-17)
- 🆕 **그린은 글자로 쓰지 않습니다.** 면·선(선택·활성) 전용. 글자는 잉크 4단
- 🆕 **선은 `--line`, 면은 `--fill`.** 섞어 쓰지 않습니다
- 🆕 **헬퍼 텍스트** `.helper` — 필드 **아래** 6px · 12.5px · `--ink-4` · 행간 1.6
- 🆕 **폼 3단 위계 (예외 없음)** `[.flabel 15px·800] → [.mfield] → [.helper]` · 간격 22 / 8 / 6px
- 🆕 **입력 라벨은 명사형**이고 「답한 것」 요약 줄의 이름과 **같은 값**을 씁니다 (G-8)
- 🆕 **Light(300) 금지.** 가장 얇은 글자는 400. 대비는 400 ↔ 900
- 🆕 **프라이머리 버튼은 화면이 달라도 같은 버튼** — 재계산 CTA도 `--green` + 잉크
- 🆕 **버튼 안 화살표 금지** — 위치가 이미 말하는 것을 반복하지 않습니다
- 🆕 **화면 노출 이모지 0개** (v23.20의 「💡 1개 허용」 조항 폐기)
- 🆕 **앱 배경 위 호버는 `--line`.** `--fill`은 `--bg`와 같은 값이라 아무 일도 안 일어납니다
- 🆕 **컴포넌트 3계층 (높이 토큰 고정)** — CTA `--h-cta` 54px·16px/600 · 옵션 `--h-opt` 46px·14px ·
  상태 칩 `--h-chip` 35px·13px. **이 셋 밖의 높이를 만들지 않습니다**
- 🆕 **타입 스케일에 0.5px 단위 금지** — `--t7`을 12.5 → **13px**
- 🆕 **모든 텍스트 입력은 16px 이상** — 미만이면 iOS가 포커스 시 화면을 확대합니다
- 🆕 **헤더는 아이콘 없이 워드마크만** — `영끌`(900) + `계산기`(500) · 자간 -.04em · 자체가 홈 버튼
- 🆕 **깊은 근거는 바텀시트로** — 결과 화면은 미니멀하게, ⓘ 하나로 접습니다.
  ⚠ 시트는 `.app` **밖**에 둡니다(`overflow-x:clip`이 fixed를 가둡니다).
  ⚠ 스크롤 잠금은 `documentElement.style.overflow`만 씁니다(`body{position:fixed}`는 위치를 튕깁니다)
- 🆕 **면책은 시트로 내리지 않습니다.** 방어 문구는 눌러야 보이면 안 됩니다
- 🆕 **판정 채널의 색 경계는 게이지 눈금 안에 있어야 합니다.** 막대 만석이 0.40이면
  색 경계도 0 < a < b < 0.40. **밖에 두면 그 색은 한 번도 안 뜹니다**(v23.26에서 실제로 그랬습니다)
- 🆕 **판정 색의 채도는 양방향으로 잠급니다 (95~150).** 한쪽만 막으면 반대편 끝으로 튑니다
- 🆕 **같은 줄의 두 버튼은 글자 크기·굵기가 같습니다.** 위계는 면이 만듭니다
- 🆕 **화면 문구 금지어**: 「정확한 금액」 · 「살짝」 · 「크게 올라」 · 「완벽」 · 「충분히」 ·
  「어느 정도」 · 「대략적으로」 — 면책과 어긋나거나 수치가 없는 표현입니다
- 🆕 **밖으로 나가는 링크는 `<a target="_blank" rel="noopener noreferrer">`.**
  `window.open`은 쓰지 않습니다 — 인앱 브라우저에서 막히거나 같은 탭에 뜹니다
- 🆕 **공유 카드도 웹과 같은 라이트 톤.** 그린은 여기서도 **글자가 아니라 브랜드 마크(세로 막대)**
- 🆕 **콤마를 다시 찍을 때 캐럿을 보존합니다** — 「콤마 앞 숫자 개수」로 자리를 되돌립니다
- 🆕 **값이 잘리면 화면으로 말합니다** — `.fielderr` · `opacity`+`max-height` 전환 ·
  평소 높이 0 · `role="alert"`. ⚠ `hidden`으로 감추면 전환이 재생되지 않습니다
- **화면 노출 이모지**: 02단계 넛지의 `💡` **1개**
- 색 총 **21개** (상한 26) — v23.24에서 시트 배경 `rgba(25,31,40,.45)` 하나 추가

⚠ 퇴출 색은 **원색만** 막습니다: `#2563EB` `#3182F6` `#0071E3` `#0091FF` `#1E3A8A`
   `#F59E0B` `#FFC342` `#DC2626` `#F04452` `#059669` `#15C47E` `#248A3D`

### 입력 01 히어로 카드

첫 화면은 `.app.hero-on` 상태에서 질문 블록이 흰 카드가 되고,
그 안의 `.moneycard`는 배경·그림자·패딩을 벗습니다(카드 안의 카드 금지).
**`margin-top`만 24px로 남겨 02·03과 리듬을 맞춥니다.**

---

## 3. 🔴 구 배포판에 있었는데 본체에 없는 것

| 기능 | 상태 |
|---|---|
| **실거래가 조회** (Vercel `api/apt-price.js`) | 🔴 **미이식** — 1순위 · 코드는 `rent.html`에 |
| **정책대출 선택 UI** | 🔴 화면만 없음. 엔진은 있음 — 2순위 |
| 상세 입력 (금리·기간·MCI·은행 자체한도) | 🔴 기본값 고정 |
| GA 사용 로그 | 🔴 미이식 |
| 전월세 | ⏸ 본체에서 제거 · `rent.html` 보관 |

### 지금 고정돼 있는 계산 조건

```js
const D = { rate:5.4, years:30, rateType:'variable', fixedYears:0 };
creditLoan:0, mciCovered:false, manualLoanCap:0,
bankSelfCap:0, extraFunding:0, firstTimeTaxCut:false, seominCheck:false,
loanChoice:'bank'
```

### 남은 오차 — 정책대출 (과소 방향)

시중은행 5.4% 기준으로만 계산해 실제보다 **약 7,657만원 과소**로 나옵니다.
**과소는 과대보다 덜 위험합니다** — 집을 덜 사게 될 뿐 계약이 깨지지 않습니다.
면책에 밝혀 두었습니다.

---

## 4. 기존 부채 입력 — v23.11에서 되살렸습니다

v23.3에서 지웠다가 **v23.11에서 02단계의 접이식 토글로 복원**했습니다.
지금은 `S.debtMonthly`가 `ctx()`의 `otherDebtMonthly`로 연결됩니다.

v23.20에서는 **그 입력 폼의 시각적 결함만** 고쳤습니다(1-3 (a)).

> 삭제 상태였을 때의 오차는 월 100만원 상환 기준 **약 1억 2,800만원 과대**였습니다.
> 원칙 28(사용자에게 유리한 오차)에 걸리는 방향이라 되살린 것입니다.

**DSR 면책은 그대로 둡니다** — 입력이 있어도 은행 자체 심사 기준까지는 못 봅니다.

---

## 5. 🔴 이제까지 난 사고 — 전부 화면을 다시 그리는 판에서

| 사고 | 원인 | 지금 막는 것 |
|---|---|---|
| 슬라이더를 만지면 맨 위로 튐 | `showResult()`가 조건 없이 `scrollTo(0,0)` | 원칙 92 · `keepScroll` |
| **집값 7억 → 준비 현금 8,740만원** | 만원 단위를 원 단위로 전달 | 원칙 93 · **G-11 합계 검증** |
| 자금 구성 막대가 통짜 검정 | 두 조각이 같은 색 | 원칙 94 · **명암 대비 ≥3:1** |
| 규제지역 다주택인데 「비율이 먼저 찼어요」 | 대출 0을 한도 도달로 오인 | `limits.LTV===0` 분기 |
| 「0 억」 입력창이 360px에서 14px 넘침 | 필드 치수 미계산 | 원칙 34 · **G-10** |
| 부대비용이 집값·세금·대출을 흔듦 | `solveMaxPrice` 재호출 | `S.lockedPrice` |
| 🆕 **부채 입력칸이 배경에 녹아 사라짐** | `--fill` = `--bg` 동일값 | **원칙 97** · 검사 2개 |
| 🆕 **칩·입력창만 자간이 퍼짐** | button·input이 상속 안 받음 | **원칙 98** · **G-13** |
| **간격 검사가 헛돌았음** | 하위 셀렉터가 기본 셀렉터에 매칭 | **원칙 99** |
| 🆕 **막대 대비 검사가 5판 동안 엉뚱한 짝을 쟀음** | `--espresso` ↔ `--navy-lift` (막대에 안 쓰는 값) | **원칙 99** · 1-4장 |
| 🆕 **라임을 글자로 쓰면 1.26:1** | 면용 값 하나만 만듦 | **원칙 102** · 면/글자 두 값 |
| **카드 분리 근거가 1개로 줄어듦** | 그림자 폐기 | **원칙 101** · G-15 + 검사 2개 |
| 🆕 **넛지 문장이 3.04:1로 안 읽힘** | 마이크로 라벨용 색을 본문에 씀 | `--ink-4` 한 단 하향 · 1-5장 |
| 🆕 **부담 게이지 초록이 「해도 된다」로 읽힘** | 「좋다」와 「허가」를 혼동 | **원칙 104** · 채도 사다리 |
| 🆕 **사보타주가 헛돌았음** | CSS 우선순위로 값이 안 바뀜 | **원칙 99** 재확인 · 9장 |
| 🆕 **여백 검사가 미디어 쿼리 값을 읽었음** | `@media` 안의 `.funnel{}`이 소스에서 먼저 나옴 | **원칙 99** · 줄머리 앵커 · 1-7장 |
| 🆕 **타이틀과 카드 글자가 2px 어긋나 있었음** | 바깥 여백·카드 패딩·세로선 폭을 따로 정함 | 세 값의 **관계**를 재는 검사 · 1-A-1장 |
| 🆕 **칩을 선택하면 상자가 1.02px 커짐** (v23.16부터) | 선택 시 `font-weight` 700 → 800 | **G-17**(렌더된 폭) · 1-4장 |
| 🆕 **`__selfcheck` 항목 검사가 주석만 봐도 통과** | 파일 전체에서 문자열을 찾음 | `ok()` **라벨만** 수집 · 1-6장 |
| 🆕 **그린 글자 금지 검사가 테두리까지 잡음** | `color:`가 `border-color:`의 일부 | 경계 붙임 · **원칙 99** |
| 🔴 **`const probe` 중복 선언 → 첫 화면 백지** | `__selfcheck` 안에서 변수명 충돌 | **브라우저 실행으로만 잡힘** · 1-A-6(c) |
| 🆕 **순서 검사가 「없는 것」을 통과시킴** | `indexOf`가 -1을 돌려주고 `-1 < n`은 늘 참 | 존재부터 셈 · **원칙 99** · 1-7장 |
| 🆕 **화면 호버가 아무 일도 안 함** | `--fill`이 `--bg`와 같은 값이 됨 | 배경 위 호버는 `--line` |
| 🆕 **부대비용 입력에 포커스하면 iOS가 확대** | 입력 글자 14px (16px 미만) | 「모든 텍스트 입력 16px 이상」 · 1-2장 |
| 🆕 **타입 스케일에 0.5px가 섞여 있었음** | `--t7:12.5px` | 「0.5px 단위 없음」 · 1-2장 |

**전부 화면을 다시 그리는 판에서 섞여 들어왔습니다.** 그래서 지침에 6층(시각 언어)이 있습니다.

### 🆕 이번 판에서 검사 자체가 헛돌았던 자리

「질문 → 입력 카드 간격 통일」 검사를 처음 이렇게 썼습니다.

```js
css.match(/\.moneycard\{[^}]*margin-top:(\d+)px/)
```

그런데 `.app.hero-on .funnel > .q .moneycard{` **도 이 패턴에 걸립니다.**
그리고 소스에서 **더 먼저 나옵니다.** 결과적으로 같은 규칙을 두 번 읽고
`base === hero`가 항상 참이 되어 **사보타주에도 통과했습니다.**

→ 줄머리(`\n\.moneycard\{`)로 앵커해 고쳤습니다.
→ **원칙 90(정적 마크업만 세면 헛돈다)이 셀렉터에서도 똑같이 나타난 자리**입니다.
→ **모든 신규 검사는 반드시 사보타주로 확인합니다.** 이번에 그것 때문에 잡혔습니다.

---

## 6. ⏸ 전월세 — 본체에서 완전히 뺐습니다

| | |
|---|---|
| **본체** | 전월세 관련 **카드·링크·문구 0건** |
| `rent.html` | 저장소에 **파일로만** 존재 (구 배포판 전체 복사본) |
| `vercel.json` | `/rent`는 살아 있음 — **주소를 아는 사람만** 접근 |

### 🔴 `rent.html`을 절대 지우면 안 되는 이유

**구 배포판(v21.30)의 유일한 복사본입니다.**

- 🔴 **실거래가 조회가 여기 살아 있습니다** — `fetch('/api/apt-price?...')` 호출부와
  매매용 UI(`buySiseLoadBtn`)까지. **1순위 이식의 원본입니다**
- 🔴 정책대출 선택 UI, 상세 입력 폼도 여기 있습니다 — 2·3순위의 원본
- ⚠ 구 판이라 새 디자인이 아닙니다. 손대려면 별도 판으로 열고 본체 로직은 건드리지 마세요

---

## 7. 알려진 구멍 (계산에 안 들어간 것)

| 항목 | 상태 |
|---|---|
| 기존 부채 **잔액** (월상환액만 입력받음) | 🔴 미반영 |
| 국민주택채권 매입비 | 🔴 미반영 — 면책에 명시 |
| 화재보험료 | 🔴 미반영 |
| 2·3·4주택 취득세 세분화 | 🔴 미구현 |
| 정책대출(디딤돌·보금자리) | 🔴 UI 없음 — 2순위 |
| 전세대출 이자 DSR / 전세보증보험료 | ⏸ 전월세 제거로 보류 |

⚠ **「완벽 반영」류 문구는 쓸 수 없습니다.** 위 목록이 이유입니다.

---

## 8. 외부 링크 — 미검증

| 링크 | 형식 | 상태 |
|---|---|---|
| 네이버 부동산 | `https://new.land.naver.com/search?sk={지역}` | 🔴 **미검증** |
| 호갱노노 | `https://hogangnono.com/search?q={지역}` | 🔴 **미검증** |
| 인테리어 | `PARTNER.interior` 비면 오늘의집 검색 | 계약 전 |
| 이사·청소 | `PARTNER.moving` 비면 네이버 검색 | 계약 전 |

⚠ **제휴가 실제로 붙으면** 표시광고법상 「광고」 표기가 필요합니다. 지금은 해당 없습니다.

---

## 9. 회귀 테스트 — `test.js` (397개)

```
node test.js                  # 같은 폴더의 yeongkkeul-calculator.html
node test.js /경로/파일.html   # 특정 파일
```

⚠ 엔진 함수를 **HTML에서 직접 뜯어옵니다.** 복사본을 두면 반드시 어긋납니다(원칙 58).

| 장 | 내용 |
|---|---|
| 1~4 | 정책 상수 · LTV · 지역/방공제 전수 · 취득세 |
| 5 | **영수증 합계 = 대출 + 현금** (G-11) |
| 6 | solveMaxPrice 왕복 · **단위 혼동 감시** |
| 7~8 | 병목 판정 · **원칙 28 유리한 오차 감시** |
| 9~14 | 스트레스 금리 · 월상환 · formatWon · 중개보수 · 방공제 · 값 위생 |
| 15~18 | 정책 신선도 · 개인정보 · `__selfcheck` 생존 · G-6 · **디자인 락 정적 대조** |
| **19** | 🔴 **화면 ↔ 엔진 단위 경계** + 문구·구조·**시각 언어** 락 |

### 검사 성장

```
247 (v23.17) → 258 → 266 → 268 (v23.18) → 283 (v23.19) → 293 (v23.20)
  → 313 (v23.21) → 329 (v23.22) → 346 (v23.23) → 364 (v23.24) → 372 (v23.25) → 380 (v23.27)
__selfcheck  14항목 → 16 → 17 → 19항목 (v23.26에서도 유지)
```

### 🆕 v23.27에서 추가·개정된 검사 (신규 13 · 개정 4)

**외부 링크 · 결과 복원**
```
밖으로 나가는 넷이 전부 앵커다 / 창을 스크립트로 열지 않는다
앵커는 href로만 연결된다 / 앵커가 버튼과 같은 모양이다
결과를 복구한다                  ← 「결과는 복구하지 않는다」를 뒤집음
결과 복구는 숫자가 성립할 때만 / 퍼널로 가면 결과 플래그가 내려간다
저장 키에 priceOverride가 없다 / 되살린 파생값도 검사한다
```

**공유 카드**
```
공유 카드가 라이트 톤이다 / 공유 카드 금액이 잉크다
공유 카드에도 그린은 면으로만 / 다크 그라데이션 토큰을 삭제했다
```

**입력 피드백**
```
콤마를 다시 찍어도 캐럿이 유지된다 / 잘린 입력을 화면으로 말한다
에러 줄이 평소에 자리를 차지하지 않는다 / 에러 줄이 부드럽게 나타난다
에러 줄을 hidden으로 감추지 않는다 / 에러 줄이 스크린리더에 전달된다
```

### v23.26에서 추가·개정된 검사 (신규 9 · 개정 4)

**판정 채널**
```
색 단계가 게이지 눈금 안에 있다   ← 「임계가 40/60인가」를 관계 검사로 대체
경고 두 색이 도달 가능하다
주의 · 위험의 채도가 95~150이다  ← 하한만 있던 락에 상한을 더함
위험이 주의보다 어둡다 (다크 레드)
```

**타이포 · 버튼**
```
문장 타이틀 행간이 1.22다 / 숫자 행간은 1.1~1.2로 조인다 / 문장 행간 > 숫자 행간
두 버튼 글자가 같다
```

**카피**
```
화면 문구에 과장·모호 표현이 없다 (금지어 7종)
연소득을 묻는 이유가 화면에 있다
「소득 미입력」의 이름이 하나다 (G-8)
```

### 🆕 v23.25에서 추가된 검사 (신규 8 · 개정 1)

```
localStorage 미사용 (기기에 남기지 않는다)   ← 「둘 다 미사용」을 반쪽만 품
임시 저장은 sessionStorage 한 키뿐이다 / 되살린 값을 다시 검사한다
결과는 복구하지 않는다 / 저장 키에 결과·파생값이 없다
처음부터 다시 계산하면 저장분을 지운다 / 빈 상태에서는 키를 만들지 않는다
저장 실패가 화면을 막지 않는다 / 복구가 첫 렌더 앞에서 일어난다
```

⚠ **이 검사들은 소스만 봅니다.** 실제 복구·오염·삭제는 **브라우저에서 확인**해야 합니다(원칙 112).

### 🆕 v23.24에서 추가·개정된 검사 (신규 17 · 개정 2)

**헤더**
```
헤더에 아이콘이 없다 / 워드마크가 홈 버튼이다 / 워드마크가 굵기 대비로 위계를 만든다
```

**컴포넌트 3계층**
```
3계층 높이 토큰이 정의돼 있다 / 메인 CTA 3종이 같은 높이 토큰을 쓴다
메인 CTA가 16px SemiBold다 / 옵션 버튼이 --h-opt · 14px이다 / 상태 칩이 --h-chip · 13px이다
버튼 높이를 px로 하드코딩한 곳이 없다   ← 여덟 셀렉터를 훑습니다
모든 텍스트 입력이 16px 이상이다        ← iOS 확대 방지
타입 스케일에 0.5px 단위가 없다
```

**바텀시트**
```
계산 기준이 ⓘ 인디케이터로 접혔다 / 바텀시트가 대화상자로 선언돼 있다
시트가 .app 밖에 있다 / 시트를 닫는 길이 셋이다 (닫기 · 배경 · ESC)
시트를 닫으면 스크롤 잠금이 풀린다 / 시트가 스크롤 위치를 옮기지 않는다 (원칙 92)
시트 안에 계산 기준 5항목이 있다
```

**개정 2**
```
하단 신뢰 지표가 회색 박스 없이 중앙 정렬   ← .trust → .trustwrap 으로 이동
두 버튼 높이가 같다 / 헬퍼가 12~13px       ← 하드코딩 px → 토큰 · 12.5 → 13px
```

**사보타주 18종 전부 🔴 확인했습니다.**

### 🆕 v23.23에서 추가·개정된 검사 (신규 17 · 개정 7)

**폼 3단 위계**
```
폼 3단 위계의 간격 규약 (22 / 8 / 6px · 안쪽이 바깥쪽보다 좁은가)
모든 입력 필드가 라벨 → 입력 → 헬퍼 순서다   ← indexOf -1 함정을 고친 자리
라벨이 명사형이다 (물음표·서술어 없음)
입력 라벨이 요약 줄의 이름과 같다 (G-8)
```

**복수 대출 자동 합산**
```
항목별 합산 아코디언이 있다 / 합산 항목이 신용대출 · 기타 둘이다
항목 합계가 메인 칸으로 들어간다 / 메인 칸을 직접 고치면 항목이 초기화된다
엔진으로 가는 값은 여전히 메인 칸 하나 / 합산 토글이 화면을 다시 그리지 않는다
퍼널 아코디언은 1종 (항목별 더하기)
```

**CTA · 타이포 · 카피**
```
재계산 CTA가 「다음」과 같은 버튼이다 / 프라이머리 버튼이 화면마다 같다
되돌아가기가 텍스트만이다 / 본문에 Light(300)를 쓰지 않는다
핵심 수치가 700 이상이다 / 어림값 넛지를 폐기했다 / 화면 노출 이모지가 0개다
```

**토큰**
```
입력 면이 #F2F4F6이다 / 카드 면이 배경 · 입력면과 다른 값
배경 위 호버에 --fill을 쓰지 않는다 / G-16이 --fill 면 전체를 본다
```

**개정 7**
```
결과 화면 아코디언은 2종     ← 파일 전체 → 결과 섹션으로 범위 축소
되돌아가기가 텍스트만이다     ← 「방향 표시가 있는가」를 뒤집음
좌 세컨더리 · 우 프라이머리   ← 우측이 검정 → 그린
폼 3단 위계의 간격 규약       ← 「질문 → 입력 카드 24px」를 대체
카드 면이 배경 · 입력면과 다른 값  ← --fill ≠ --bg 축 상실
```

**사보타주 21종 전부 🔴 확인했습니다.** 그중 하나(`indexOf` -1)는 처음에 조용히 통과했습니다.

### 🆕 v23.22에서 추가·개정된 검사 (신규 18 · 개정 12)

**그린 단일화**
```
그린 토큰이 단 하나다 / 그린을 글자색으로 쓰지 않는다 / 그린은 면 · 선으로만 쓰인다
금액 글자가 잉크다 / 스위치 켜짐이 브랜드 그린 / CTA 호버가 색을 바꾸지 않는다
```

**3중첩 타파**
```
입력칸에 흰 베젤이 없다 / 질문 블록이 01·02·03 모두 같은 흰 카드다
카드에 테두리를 두르지 않는다 / 질문 → 입력 카드 간격이 24px 하나로만 정의된다
```

**테두리 전역 규칙**
```
입력 필드는 테두리 0 · 회색 면 / 입력 포커스가 레이아웃을 흔들지 않는다
선택 3종이 같은 테두리 문법을 쓴다 / 활성 상태는 테두리 색만 바꾼다
선택해도 글자 굵기를 바꾸지 않는다 / 조건 칩도 헤어라인을 갖는다
면 토큰(--fill)을 선으로 쓰지 않는다 / 독의 이전 버튼이 흰 면 + 헤어라인
```

**헬퍼 텍스트**
```
헬퍼 텍스트가 입력칸 아래에 종속된다 / 헬퍼가 12~13px · 쿨 그레이다
필드 밑 문구가 4~8px 안에 붙는다
```

**정리**
```
죽은 규칙 .cta.ghost · .textbtn 없음
__selfcheck 항목 검사가 ok() 라벨만 본다   ← 주석만 남아도 통과하던 것을 고침
```

### 🆕 `__selfcheck` 신규 2항목

| 항목 | 무엇을 보나 | 어디서 돌려야 재나 |
|---|---|---|
| **G-16** 입력 필드가 뒤 배경과 구분된다 | 부모를 거슬러 **실제로 칠해진 면**을 찾아 대비 ≥1.05:1 | **입력 01 · 02** |
| **G-17** 선택해도 칩 상자가 안 커진다 | 보이는 칩을 켰다 껐다 하며 **렌더된 폭** (허용 0.5px) | 칩이 있는 화면 |

⚠ **측정 대상이 0이면 값 칸에 🟡 를 찍습니다.** G-14가 결과 화면에서 조용히 통과하던 함정을
   G-16·G-17에서 반복하지 않으려고 넣었습니다. **초록이어도 잰 게 아닐 수 있습니다.**

**사보타주 23종 전부 🔴 확인했습니다.** 그중 셋은 처음에 헛돌아 사보타주 자체를 고쳤습니다.

### 🆕 v23.21에서 추가된 검사 (신규 20 · 개정 2)

**개방감**
```
바깥 여백이 16px 인셋 (⚠ 줄머리 앵커) / 카드 안쪽 여백이 20px
히어로 카드도 같은 안쪽 여백 / 타이틀과 카드 글자의 시작점이 맞는다
```

**하단 조작 위계**
```
되돌아가기 · 재계산이 가로 2분할이다 / 가로 2분할 비율이 35 : 65다
두 버튼 높이가 같다 / 좌 세컨더리 · 우 프라이머리로 갈린다
세컨더리 면이 앱 배경과 다른 값
```

**세로선 폐기 · 순색화**
```
안내 박스에 왼쪽 세로선이 없다 (border-left 전면 0)
결과 헤더의 이유 줄이 세로선 대신 면이다 / 초과 안내도 면으로 말한다
틴트 두 값이 정의돼 있다 / 경고 틴트가 --warn에서 파생됐다
주의 · 위험이 순색이다 (채도 ≥ 110) / 탁한 밤색 잔재 없음
```

**카피 다이어트**
```
면책이 한 줄이다 / 면책 한 줄에 추정치 · 정책 변동이 모두 들어 있다
다음 걸음 문구에 지역명을 반복하지 않는다 / 작은 안내 문구 행간이 1.6 이상
```

**개정 2**
```
되돌아가기 문구에 방향 표시가 있다   ← 「← 이전 단계로 돌아가기」 → 「← 이전 단계」
재계산 CTA가 있다                    ← 이름만 (더 이상 풀폭이 아님)
```

⚠ **신규 20개 전부 사보타주 16종으로 확인했습니다.** 값을 되돌린 사본을 만들어
   실제로 🔴가 뜨는지 봤고, 그 과정에서 「바깥 여백」 검사가 헛돌던 것을 잡았습니다(1-7장).

### v23.20에서 추가·개정된 검사 (신규 12 · 개정 20)

**팔레트·문법 롤백 (신규)**
```
배경이 쿨 그레이 / 카드가 퓨어 화이트 / 웜 베이지 잔재 없음
카드가 그림자로 분리된다 / 카드에 굵은 검정 테두리가 없다
오프셋 하드 섀도우 전면 폐기 / 앰비언트 섀도우가 정의돼 있다
```

**판정 채널 재정의 (명도 사다리 검사 1개 → 4개)**
```
부담 낮음이 무채색이다 (초록 금지)   ← 「그린 계열이다」를 뒤집음
경고 2단이 난색이다 (R>G>B)
주의가 위험보다 옅다
판정 3색이 흰 면 위에서 읽힌다 (≥4.5:1)
```

**레이아웃·문구 (신규)**
```
독이 가로 2분할이다 / 독에 캡슐 껍데기가 없다
아코디언이 투명 면 + 헤어라인이다 / 규제지역 안내가 주의 위계다
슬라이더 손잡이가 흰 원 + 그림자
원리금 넛지 카피가 있다 / 정책 변동 고지가 있다 / 면책은 두 줄 이하
큰 타이틀 자간이 -.05em / 큰 금액 자간이 -.04em / 단위 span에 숨통이 있다
```

**전부 사보타주 검증했습니다.** 10종의 되돌린 사본을 만들어 실제로 🔴가 뜨는지 확인했습니다.

⚠ **사보타주 하나가 처음에 헛돌았습니다.** 아코디언 검사를 확인하려고
   `background:var(--fill)`을 **앞에** 붙였는데, 뒤에 있는 `background:transparent`가
   CSS 우선순위에서 이겨 **아무것도 안 바뀐 채 통과**했습니다.
   → **사보타주도 실제로 값이 바뀌었는지 확인해야 합니다**(원칙 99의 재확인).

### `__selfcheck()` — 17항목

| 항목 | 무엇을 보나 |
|---|---|
| **G-13** 자간이 button·input까지 닿음 | 프로브 요소의 **렌더된 computed style** |
| **G-14** 선택해도 테두리가 굵어지지 않음 | `.is-on`의 실제 border 폭 **≤ 1.5px** |
| **G-15** 배경 · 면 토큰이 서로 다른 값 | `--fill ≠ --bg` · `--card ≠ --bg` (원칙 97) |
| 막대 두 조각 대비 | **실제 두 조각(`--ink` ↔ `--green`)** — 7.64 : 1 |

⚠ **G-14는 결과 화면에서 0개로 통과합니다.** 입력 03에서 한 번 더 돌리세요.

### `test.js`와 `__selfcheck()`의 역할 분담

| | 무엇을 보나 | 언제 |
|---|---|---|
| `node test.js` | 계산 엔진 + 화면 **소스**의 단위·구조·문구·토큰 | 코드를 고칠 때마다 |
| `__selfcheck()` | 브라우저에서 **실제 렌더된 값** | 배포 확인할 때 · 결과 + 입력 03 |

**둘 다 못 보는 것: 눈에 보이는 모양.** 판마다 실기 캡처를 한 번은 봐야 합니다.
「막대가 통짜 검정」·「인테리어 카드 터짐」·「0만이 허공에 뜸」은 전부 캡처로만 잡혔습니다.

---

## 10. 사용자(노아초이) 작업 스타일

- **먼저 측정하고 시작할 것.** 인상 대신 숫자로 시작하면 대화가 빨리 붙습니다
- **선택지를 줄 때는 권장을 명시**하고, 안전한 절충안보다 판단상 가장 나은 안을 실제로 구현해 보여줄 것
- **구조를 바꾸는 제안은 먼저 옵션을 제시하고 승인받은 뒤 구현**
- **지시를 임의로 절충하지 말 것.** 원칙상 우려가 있으면 **한 번 말하고, 그래도 지시가 유지되면 그대로 따른다.**
  대신 그 판단의 근거와 숫자를 이 문서에 남긴다 (1-5장이 그 예)
- **외부 제안(제미나이 등)은 이전 판의 맥락을 모릅니다.** 「무단 추가」로 보이는 것이
  직전 판에서 승인받은 안전장치일 수 있으니, 지우기 전에 이 문서를 확인하세요
- 🆕 **외부 제안의 진단과 처방은 나눠서 받습니다.** 이번 판에서 **진단 7건은 전부 정확했고
  처방 2건이 틀렸습니다.** 증상 설명을 믿고, 원인은 직접 다시 찾으세요
- **여러 지적이 한 번에 오면 증상별이 아니라 원인별로** 묶어서 답할 것
- GitHub 웹 업로드로 작업(터미널 git 없음), 모바일에서 배포 결과를 직접 확인
- 응답은 **구조화된 한국어 · 표 선호**

---

## 11. 파일

| 파일 | 용도 |
|---|---|
| `yeongkkeul-calculator.html` | **본체 (v23.27)** — `/` |
| `rent.html` | 🔴 **구 배포판(v21.30) 전체 복사본** — `/rent` · 실거래가·정책대출 코드 원본 |
| `vercel.json` | 주소 ↔ 파일 연결 |
| `test.js` | 회귀 테스트 397개 |
| `영끌계산기_지침_v7.md` | 원칙 정본 |
| `영끌계산기_인수인계_v23_27.md` | 이 문서 |
| `api/apt-price.js` | 실거래가 Vercel 함수 — **살아 있음** |

---

## 12. 다음 세션 첫 메시지 추천 문구

> 다섯 파일 첨부했습니다 (지침 v7 · 인수인계 v23.24 · 본체 · test.js · **rent.html**).
> 착수 전에 `node test.js`(364개)를 돌려 주세요.
> 본체(v23.24)에 **실거래가 API를 이식**해 주세요. 호출부는 `rent.html`에 있습니다.
> 이식 후 다시 364개 + 신규 케이스를 돌리고, `__selfcheck()`는 결과 · 입력 03 · **입력 02** 세 곳에서 돌려 주세요.
> ⚠ 인수인계 1-5장에 **결정 대기 1건**(세션 상태 복구 A/B)이 있습니다.

---

## 13. 이번 세션 요약 (2026.08.10~11)

### 판 이동
`v23.17` → … → `v23.25` → `v23.26` → **`v23.27`**. **열한 판. 엔진·정책 수치 무변경.**

### 검사 성장
```
247 → 258 → 266 → 268 → 283 → 293 → 313 → 329 → 346 → 364 → 372 → 380 → 397   (test.js)
14 → 16 → 17 → 19 항목                                                        (__selfcheck)
```

### 🔴 v23.27에서 확인한 것

| | 결과 |
|---|---|
| `node test.js` | **397 / 397 ✅** |
| `__selfcheck()` | **19 / 19 ✅** · 색 **20개**(상한 26) |
| 외부 링크 4종 | `A` · `_blank` · `noopener noreferrer` · href 정상 ✅ |
| **결과 복원** | 새로고침 → 결과 화면 유지 · `lockedPrice` 동일 · 금액 동일 ✅ |
| **캐럿 보존** | 「12,345」 가운데 삽입 → `12,934` · 커서 4 ✅ |
| **에러 줄** | 6자리 입력 → 안내 표시 · 전환 중 opacity 0.47 ✅ |
| 공유 카드 | 상단 `rgb(242,244,246)` · 글자 `rgb(25,31,40)` ✅ |
| 사보타주 | **17종 전부 🔴 발생 ✅** |

### 새로 얻은 원칙 (지침 v8 후보)

| # | 원칙 후보 | 어디서 |
|---|---|---|
| 105~108 | 여백 · 분리 근거 · 접근성 변형 · 줄머리 앵커 | v23.21 |
| 109~112 | 그린 글자 금지 · 굵기가 상자를 넓힘 · 주석 함정 · 스크립트 죽음 | v23.22 |
| 113~116 | 라벨 이름 일치 · 보조 입력 · `indexOf` -1 · 토큰 동값 | v23.23 |
| 117~120 | 아이콘의 기능 이전 · 높이 토큰 셋 · 16px는 iOS 동작 · 감추기와 옮기기 | v23.24 |
| 121~124 | 저장은 「언제 사라지는가」 · 되살린 값은 입력값 · 파생값 미저장 · 지우기와 다시 쓰기 | v23.25 |
| 125~128 | 색 경계는 눈금 안에 · 양방향 락 · 주석의 결함 · 형용사와 면책의 온도 | v23.26 |
| **129** | **밖으로 나가는 길은 네이티브 앵커로 만든다** — 스크립트 창은 인앱 브라우저에서 막힌다 | v23.27 |
| **130** | **「다시 부르지 않는다」와 「돌려주지 않는다」는 다르다** — 한 번 정한 값은 돌려줘도 된다 | v23.27 |
| **131** | **조용히 자르지 않는다** — 잘랐으면 화면으로 말한다 | v23.27 |
| **132** | **전환은 `hidden`과 함께 쓰지 못한다** — 항상 DOM에 두고 클래스로 편다 | v23.27 |

⚠ **지침 v8을 만들 때 위 스물여덟을 정식 번호로 올리세요.** 지금은 이 문서에만 있습니다.

### 아직 안 한 것 (중요도 순)

| | 항목 | 왜 |
|---|---|---|
| **0** | **v23.27 실기 확인 → 배포** | 🔴 **열한 판이 쌓였습니다. 페이즈 2 전에 반드시 배포하세요** |
| **1** | **실거래가 API 이식** | 🔴 **페이즈 2 전체가 이것 위에 올라갑니다** (14장) |
| 2 | **정책대출 UI** | 지금 **7,657만원 과소** 산출 |
| 3 | 상세 입력 (금리 · 기간 · MCI) | 지금 5.4% · 30년 고정 |
| 4 | 네이버 · 국토부 딥링크 실검증 | 작업 환경에서 도메인 접근 불가 |
| 5 | 화재보험료 · 국민주택채권 매입비 | 면책에만 명시 |
| 6 | 지침 v8 (원칙 105~132 승격) | 지금은 이 문서에만 |
| 7 | 「갚고 있는 대출 입력 닫기」 링크 문구 | 필드에 라벨이 생겨 조금 겹칩니다 |

---

## 14. 🟡 페이즈 2 설계 — 캐스케이딩 실거래가 추천

**착수 전입니다.** 아래는 다음 세션이 그대로 집어 들 수 있게 정리한 설계입니다.

### 14-0. 🔴 착수 조건 — 이것부터입니다

```
[0] v23.27 배포           ← 열한 판이 쌓였습니다. 페이즈 2 전에 반드시
[1] api/apt-price.js 이식  ← 지금 본체에 없습니다. rent.html에만 있습니다
[2] 응답 필드 확정         ← 무엇을 받을 수 있는지 코드로 확인
[3] 캐스케이딩 매칭
[4] 취향 칩
```

⚠ **다음 세션에 `rent.html`을 반드시 함께 첨부하세요.** 호출부·응답 처리·API 키 위치가 그 안에 있습니다.

### 14-1. 🔴 [2]가 페이즈 2의 진짜 첫 관문입니다

지시안의 취향 칩 넷 중 **국토부 아파트 매매 실거래가로 만들 수 있는 것은 하나**로 보입니다.

| 칩 | 필요한 값 | 실거래가 API |
|---|---|---|
| **신축** | 건축년도 | ✅ 응답에 있습니다 (`buildYear`) |
| 1,000세대 | 단지 총 세대수 | ❌ 없습니다 — 단지 정보 API 별도 |
| 역세권 | 역 좌표 + 단지 좌표 | ❌ 없습니다 — 별도 데이터셋 + 거리 계산 |
| 학군 | 배정 정보 | ❌ 없습니다 — 정의 자체가 주관적 |

> ⚠ **이 표는 제 기억 기준이며, 이 작업 환경에서는 공식 명세를 열 수 없습니다.**
> **`rent.html`의 실제 응답 처리 코드로 확정해야 합니다.**
> 지침 — API 명세는 **공식 자료로 검증한 것만** 반영하고, 못 찾으면 **넣지 않고 못 찾았다고 말합니다.**

**권장:** 1차는 **신축 하나**로 냅니다. 나머지 셋은 출처를 확보한 뒤 하나씩 켭니다.
데이터 없이 「역세권」을 붙이면 **지어낸 값**이 됩니다. 이 도구에서 가장 하면 안 되는 일입니다.

### 14-2. ⚠ 이모지 칩은 현재 락과 충돌합니다

지시안은 `[🚇 역세권] [🏫 학군] [🏢 1,000세대] [✨ 신축]`입니다.
**v23.23에서 「화면 노출 이모지 0개」를 락으로 걸었습니다.**

| | 결과 |
|---|---|
| **A. 글자만** `[신축]` | 락 유지 · 옵션 칩(46px) 컴포넌트를 그대로 씁니다 |
| B. 이모지 허용 | 락을 다시 써야 합니다. 칩 폭이 20~30% 늘어 한 줄에 안 들어갑니다 |

**권장은 A입니다.** B로 가겠다고 하시면 락을 새 규칙으로 바꿔 쓰고 반영합니다(한 번만 말합니다).

### 14-3. 캐스케이딩 3단계 — 채택. 다만 문구는 사실만

| 단계 | 조건 | 문구 방향 |
|---|---|---|
| Direct Hit | 선택한 시·군·구에 예산대 거래 있음 | 「최근 N개월 실거래 M건」 — **건수를 밝힙니다** |
| Proximity | 같은 구에 거래가 적음 | 「인근 ○○동에는 M건 있어요」 |
| Wide | 예산과 시세 차이가 큼 | 「이 예산으로는 ○○구에 M건 있어요」 |

⚠ **3단계를 「서울 안에서」로 고정하면 안 됩니다.** 이 도구는 전국 16개 시·도를 다룹니다.
   서울 사용자에게만 서울, 나머지는 **그 시·도 안에서** 대안을 찾습니다.
⚠ **「추천」이라는 말을 쓰지 않습니다.** 우리가 하는 일은 **거래가 있었던 곳을 보여주는 것**입니다.
   지침 말투 — 평가하거나 지시하지 않고 사실만 서술합니다.
⚠ **0건 화면을 먼저 정하세요.** 실거래가는 자주 비어 있습니다. 빈 화면이 기본값입니다.

### 14-4. 화면 구조 — 기존 규격 안에서

```
결과 화면
 └ [카드] 다음 걸음                    ← 지금 있는 2×2 자리
 └ [카드] 이 예산으로 거래된 곳         ← 신설 (4번째 카드)
      최근 3개월 · 서울 종로구 · 12건
      ┌───────────────────────────┐
      │ ○○아파트   84㎡   3억 9,500만  │  ← .mini 재사용
      │ ○○아파트   59㎡   3억 8,700만  │
      └───────────────────────────┘
      [신축]                            ← 옵션 칩 46px · 토글
      ⓘ 어떤 거래를 보여주나요           ← 바텀시트 재사용
```

🔒 **새 컴포넌트를 만들지 않습니다**(원칙 118). 카드·`.mini`·옵션 칩·바텀시트를 그대로 씁니다.
⚠ **G-9(결과 블록 ≤ 8)**: 지금 7개입니다. 카드 하나를 더하면 **8개 — 상한입니다.**
   그다음에 무엇을 더하려면 **무엇을 접을지 먼저 정해야 합니다**(4층 3번).

### 14-5. 데이터·비용 — 페이즈 2에서 반드시 정할 것

| 항목 | 왜 지금 정해야 하나 |
|---|---|
| **캐싱** | 같은 지역·같은 달을 매번 부르면 호출량이 사용자 수만큼 늘어납니다. Vercel 함수에서 **지역코드+년월 단위로 캐시**하세요 |
| **호출 한도** | 공공데이터 API는 일일 한도가 있습니다. 한도를 넘기면 **결과 화면이 통째로 죽습니다** — 실패해도 계산기는 그대로 돌아야 합니다 |
| **API 키** | 클라이언트에 노출하지 않습니다. Vercel 함수 안에서만 씁니다 (지금 구조가 이미 그렇습니다) |
| **응답 지연** | 결과 화면을 **막지 않습니다.** 계산은 즉시 보여주고 매물은 나중에 채웁니다 |
| **개인정보** | 실거래가 호출에 **소득·현금을 보내지 않습니다.** 지역코드와 가격대만 보냅니다(원칙 36) |

### 14-6. 브랜드 — 코드에 남길 문장 하나

> **The Compass, Not the Navigator.**
> 정답을 주지 않고 **가장 정확한 출발선**을 줍니다.

이 문장은 이미 코드가 지키고 있습니다 — 「과소는 과대보다 낫다」(원칙 28),
「완벽 반영 문구 금지」(7장), 「평가하지 않고 사실만」(말투 규칙).
**페이즈 2에서 「추천」·「최적」·「딱 맞는」이 들어오면 이 포지셔닝이 깨집니다.**

### 14-7. 다음 세션 첫 메시지 (페이즈 2용)

> 여섯 파일 첨부했습니다 (지침 v7 · 인수인계 v23.27 · 본체 · test.js · **rent.html** · api/apt-price.js).
> `node test.js`(397개) 먼저 돌려 주세요.
> **`rent.html`의 실거래가 호출부와 응답 필드부터 읽고**, 무엇을 만들 수 있는지 알려 주세요.
> 그다음 본체에 이식하고, 캐스케이딩 매칭은 그 위에 올립니다.
